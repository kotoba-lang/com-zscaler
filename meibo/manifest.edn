{:actor/id        "meibo"
 :actor/glyph     "名"
 :actor/tier      :tier-b
 :actor/integrates ["saisei" "tate" "toritsugi"]
 :actor/domain    "meibo.etzhayyim.com"
 :actor/did       "did:web:etzhayyim.com:actor:meibo"
 :actor/purpose   "Verified legal-institution directory registry — the honest, lawful implementation of gftdcojp ADR-0016's never-built judge/bengoshi/adr/legal-aid vision. Institution-level LINKS ONLY (bar-association registers, court locators, Japan-specific licensed-scrivener registries, UK insolvency-practitioner register) — never individual professional records. Every URL verified live (WebSearch/WebFetch) before being recorded, never guessed. Serves as the standalone, growing reference the wider ecosystem (saisei, tate, toritsugi) can point visitors to when they need a REAL licensed professional or court, rather than replicating personal-data registries that stay opt-in/self-reported/ToS-protected at their source of truth."
 :actor/status    :r0 ; R0 seed — 10 jurisdictions (2607062200)
 :actor/adr       "2607062200"
 :actor/jurisdictions [:jp :us :uk :de :kr :fr :au :ca :it :es] ; R0 seed — ~183 remaining named as worklist by coverage_report (G10)
 :actor/related   ["2607061800"   ; saisei (the :legal_directory pattern this actor generalizes)
                   "2606112301"   ; tate R0 (30-jurisdiction :juris/referrals precedent meibo overlaps)
                   "2606112400"   ; tate worldwide
                   "2607022300"]  ; unified actor deploy (per-actor split — motivates meibo's self-containment)

 :actor/labor
 {:isic   "N8299"
  :isco   ["3411"]
  :unspsc ["80121600"]
  :lps-rank nil}

 :actor/charter-rider {:rider/version "3.6" :rider/applied true}

 :actor/gates
 [{:gate/id "G1" :gate/name "institution-level-only" :gate/req "no individual professional records (bar number, disciplinary history, docket) — only the institution's OWN official search-tool URL; the visitor looks someone up on the institution's own site, under its own terms"}
  {:gate/id "G2" :gate/name "non-adjudicating" :gate/req "meibo never asserts a professional is competent, in good standing, or a good fit — it points at the authoritative place to check"}
  {:gate/id "G10" :gate/name "jurisdiction-honesty" :gate/req "every :dir/url verified live before being recorded (never guessed/remembered); coverage_report.cljc names uncovered jurisdictions as an explicit worklist, never silently omits them"}]

 :actor/non-goals
 [{:ng/id "N1" :ng/req "does not ingest, cache, or republish individual professional records — those stay on the institution's own site"}
  {:ng/id "N2" :ng/req "does not rank, recommend, or vouch for any specific professional/firm"}
  {:ng/id "N3" :ng/req "does not become a code-level dependency of saisei/tate/toritsugi — every Tier-B actor here is split into its own repo by actor:publish (ADR-2607022300); meibo stays self-contained (own edn.cljc) so that split never breaks a consumer"}]

 :actor/cells [] ; link-registry only — no LangGraph cells (no proposal/decision flow to gate)

 :actor/lex
 [{:lex/id "directoryEntry"}]} ; com.etzhayyim.apps.meibo.directoryEntry (public — institution-level, no PII)
