{:actor/id        "mitooshi"
 :actor/glyph     "見通し"
 :actor/display-name "見通し — Probabilistic Forecasting Observatory"
 :actor/tier      :tier-b
 :actor/integrates ["kanjo"]
 :actor/domain    "mitooshi.etzhayyim.com"
 :actor/did       "did:web:etzhayyim.com:actor:mitooshi"
 :actor/purpose   "Probabilistic FORECASTING OBSERVATORY — the charter-clean answer to 'a quant-market / Google-Trends future-prediction actor + the architecture that corrects model error from fact, online'. NOT a trading bot (its constitutional inverse): a naive quant predictor IS profit speculation (Charter §1.3 non-profit + yobel 'predictive market / derivative speculation' prohibition). mitooshi instead emits probability distributions over PUBLIC time-series (chokepoint transit-load, congestion, availability, flow-rate, price-index, search-interest) routed to resilience / planning / early-warning (danjo/kanae/watari/watatsuna siblings), and NEVER places a trade, holds a position, or derives P&L. It does not adjudicate or advise (kanjo boundary — 投資助言業 ではない). 見通し (outlook/visibility) = visibility into a distribution of possible futures, not a prophecy of one. The 'fact→error→weight→learn' loop is structural: a forecast carries its info-AS-OF stamp, the observation arrives later as an append-only datom, and the proper-scoring residual (CRPS/pinball/Brier/log-score) is the join across kotoba `as-of` — so a backtest can never see future data (look-ahead leak is STRUCTURALLY impossible on an append-only log). Residuals drive an EWMA bias + variance-inflation recalibration whose real training substrate is baien federated edge (Murakumo-only). Charter-clean BY CONSTRUCTION: distribution-only (:point-asserted :db/allowed [false], G1, 非終末論), non-speculative use (:use enum excludes trade/wager, G2), primary-public sources only (:source-class enum excludes proprietary terminals + scraped Trends, G4), promotion only when the model beats a baseline AND is calibrated AND is member-signed (G7/G9/G12)."
 :actor/status    :r0
 :actor/adr       "2606051800"
 :actor/related   ["2606041827" "2606012600" "2605302300" "2605301600" "2606032000" "2605242600" "2605242630" "2605241900" "2605215000" "2605231525" "2605312345" "2605262130" "2605192100" "2605192200"]
 :actor/labor
 {:isic   ["M7220"]
  :isco   ["2120" "2511"]
  :unspsc ["81"]
  :lps-rank nil}

 :actor/charter-rider {:rider/version "2.0" :rider/applied true}

 :actor/gates
 [{:gate/id "G1" :gate/name "distribution-only-no-point-assertion" :gate/req ":forecast/point-asserted is :db/allowed [false] (schema) + const false (lexicon) + ValueError (forecast_issue/score). A deterministic single-future is unrepresentable — every forecast is a probability distribution. The 非終末論 invariant made structural: no final-state datom."}
  {:gate/id "G2" :gate/name "non-speculative-no-trading" :gate/req ":forecast/use ∈ {resilience planning nowcast early-warning research}; trade/speculation/wager/position are NOT enum members. mitooshi never places an order, holds a position, or settles money (Charter §1.3 + yobel prohibition). The inverse of a quant trading bot."}
  {:gate/id "G3" :gate/name "non-adjudicating" :gate/req "forecasts are observations routed to planners (danjo/kanae/watari); mitooshi does NOT decide, rate, value, or advise (kanjo boundary — not 投資助言業). silenMitooshiReview never authorizes a trade."}
  {:gate/id "G4" :gate/name "primary-public-source-only" :gate/req ":series/source-class ∈ {public-broadcast primary-disclosure open-commons gov-open-data member-principal}. Proprietary terminals (Bloomberg/CapIQ/Refinitiv/四季報) + scraped Google-Trends are NOT representable (kanjo §2(c)/(e)); Trends only via karakuri member-principal ToS-honest path, recorded :member-principal + G10-gated."}
  {:gate/id "G5" :gate/name "leak-free-skill-honest-scoring" :gate/req "every :forecast carries :info-as-of; score_pair RAISES if the obs is not STRICTLY AFTER it (look-ahead leak is impossible on an append-only log). Scoring uses proper scoring rules ONLY (CRPS/pinball/Brier/log-score); skill is measured against a documented baseline, never cherry-picked accuracy."}
  {:gate/id "G6" :gate/name "murakumo-only" :gate/req "all inference via LiteLLM 127.0.0.1:4000 (ADR-2605215000)"}
  {:gate/id "G7" :gate/name "calibration-honest" :gate/req "reliability (PIT mean + L1 deviation) is reported; calibration_gate REFUSES promotion of a miscalibrated/overconfident model. No hidden overconfidence."}
  {:gate/id "G8" :gate/name "baien-edge-online-update" :gate/req "weight correction (bias + variance inflation) runs on baien federated edge (:runtime :baien-edge, edge envelope ADR-2605241900); commercial-GPU runtimes (RunPod/etc.) are rejected by online_update (ADR-2605215000)."}
  {:gate/id "G9" :gate/name "no-server-key" :gate/req "model promotion (a retrained version going live) = member/operator signature; server signature refused (ADR-2605231525); serverHeldKey const false."}
  {:gate/id "G10" :gate/name "outward-gated" :gate/req "live data ingest + live forecast publish + live model promotion + live federated backward pass all Council Lv6+ + operator gated; R0 = offline replay + scoring + simulated update only."}
  {:gate/id "G11" :gate/name "sourcing-honesty" :gate/req "all seed :representative; figures flagged; require primary-source verification before outward use."}
  {:gate/id "G12" :gate/name "anti-pseudoscience" :gate/req "a model is :fc.model/skilled true ONLY if it beats a documented baseline (climatology/persistence/random-walk) on a proper score; astrology/波動/技術的占い have no representation (kizashi G10 precedent). Honest :skilled false otherwise."}]

 :actor/non-goals
 [{:ng/id "N1" :ng/req "NOT a trading/quant bot — never places an order, holds a position, or derives P&L (G2; Charter §1.3; yobel prohibition)"}
  {:ng/id "N2" :ng/req "no investment advice / rating / valuation / 業績予想 (G3; kanjo boundary — not 投資助言業)"}
  {:ng/id "N3" :ng/req "no proprietary-terminal or scraped-Google-Trends ingest (G4 — unrepresentable; Trends only via karakuri member-principal)"}
  {:ng/id "N4" :ng/req "no person-level prediction / pattern-of-life — series observe systems, never people (watari G4 precedent)"}
  {:ng/id "N5" :ng/req "no point-prophecy / deterministic final-state forecast (G1 — 非終末論, unrepresentable)"}
  {:ng/id "N6" :ng/req "no commercial-GPU training (G8 — baien-edge only, ADR-2605215000)"}
  {:ng/id "N7" :ng/req "no server-signed model promotion (G9 — no-server-key, ADR-2605231525)"}]

 :actor/cells
 [{:cell/id "series_ingest"    :cell/kind :langgraph :cell/runtime :wasm :cell/node "reuben"}
  {:cell/id "forecast_issue"   :cell/kind :langgraph :cell/runtime :wasm :cell/node "simeon"}
  {:cell/id "backtest_score"   :cell/kind :langgraph :cell/runtime :wasm :cell/node "levi"}
  {:cell/id "online_update"    :cell/kind :langgraph :cell/runtime :wasm :cell/node "judah"}
  {:cell/id "calibration_gate" :cell/kind :langgraph :cell/runtime :wasm :cell/node "zebulun"}]

 :actor/lex
 [{:lex/id "seriesObservation"}
  {:lex/id "forecastDistribution"}
  {:lex/id "scoreResidual"}
  {:lex/id "calibrationReport"}
  {:lex/id "modelUpdateAttestation"}
  {:lex/id "silenMitooshiReview"}]}
