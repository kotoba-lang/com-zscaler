{:actor/id        "saisei"
 :actor/glyph     "生" ; 再生 (saisei) — rehabilitation/rebirth
 :actor/tier      :tier-b
 :actor/integrates ["tate" "yobel"]
 :actor/domain    "saisei.etzhayyim.com"
 :actor/did       "did:web:etzhayyim.com:actor:saisei"
 :actor/purpose   "Citizen self-filing debt-relief CONCIERGE — the debtor-initiated counterpart to tate's creditor-side :insolvency track. A member who wants to petition for their OWN formal insolvency relief (自己破産・個人再生・US Chapter 7/13・UK bankruptcy/DRO・DE Verbraucherinsolvenz) gets a coded, jurisdiction-keyed registry disclosing: eligibility SIGNALS (never a verdict), the filing forum, required forms/documents checklist, statutorily MANDATORY pre-filing steps modeled as blocking (never skippable), fee + fee-waiver route, DISCLOSED discharge-timeline rule (never a computed date), and always-present referral to the jurisdiction's free/public insolvency-counseling directory. UPL: the member assembles, signs and files THEMSELVES; saisei never drafts the petition or represents; high-stakes (asset/means-test complexity, contested filings) always referral-forward."
 :actor/status    :r0 ; R0 seed — jp/us/uk/de (2607061800)
 :actor/adr       "2607061800"
 :actor/jurisdictions [:jp :us :uk :de] ; R0 seed — remaining ~189 named as worklist by coverage_report (G10)
 :actor/related   ["2606112301"   ; tate R0 (UPL-safe concierge prior art)
                   "2606112400"   ; tate worldwide (jurisdiction-registry pattern)
                   "2605262700"   ; chigiri (legal-procedure substrate, UPL prior art)
                   "2605201800"   ; yobel (voluntary doctrinal debt release — sibling, not overlapping)
                   "2605202000"   ; amnesty (legal-person institutional restructuring — sibling, not overlapping)
                   "2605312030"   ; toritsugi (adjacent proactive concierge, different institutional target)
                   "2606112201"   ; kaiyaku (縁-ledger prepayment-as-claim cross-check)
                   "2606060900"   ; tasuke (crisis/fraud-recovery routing, referenced for acute-distress cases)
                   "2605231525"   ; no-server-key
                   "2605215000"   ; Murakumo-only
                   "2605181100"   ; encrypted consent envelope (live member docs)
                   "2605312345"]  ; kotoba Datom = canonical state

 :actor/labor
 {:isic   "N8299"
  :isco   ["3411"]
  :unspsc ["80121600"]
  :lps-rank nil}

 :actor/charter-rider {:rider/version "3.6" :rider/applied true}

 :actor/gates
 [{:gate/id "G1" :gate/name "member-principal-own-situation-only" :gate/req "discloses procedure for ONLY the member's own debt situation, under member consent; live member data is encrypted (com.etzhayyim.encrypted.*); R0 ships SYNTHETIC seeds"}
  {:gate/id "G2" :gate/name "non-adjudicating-signal-not-verdict" :gate/req "eligibility output is pattern + DISCLOSED statutory anchor + :verify-current-law; saisei NEVER asserts qualification/discharge outcome; report language stays 可能性/専門家確認"}
  {:gate/id "G3" :gate/name "upl-self-file-only" :gate/req "弁護士法72条 + equivalents: no representation, no drafting/filing on the member's behalf (make-option raises on :representation); required forms/checklist come verbatim from the coded registry; the MEMBER assembles, signs and files"}
  {:gate/id "G4" :gate/name "timeline-honesty" :gate/req "NEVER computes a calendar deadline/discharge date; emits the DISCLOSED rule text + statutory anchor + verify-current-law=true"}
  {:gate/id "G5" :gate/name "mandatory-precondition-honesty" :gate/req "a jurisdiction's statutorily-required pre-filing step (e.g. DE außergerichtlicher Einigungsversuch, US §109(h) credit counseling) is modeled as a BLOCKING step in the plan — the plan cannot reach self-file without it where the law requires it"}
  {:gate/id "G6" :gate/name "no-outcome-promise" :gate/req "never states or implies a discharge will be granted; discharge is ALWAYS framed as the court's decision, saisei only discloses the disclosed rule + anchor"}
  {:gate/id "G7" :gate/name "referral-forward" :gate/req "every plan carries the jurisdiction's free/public insolvency-counseling directory (法テラス / US credit-counseling agencies / UK MoneyHelper+Citizens Advice / DE Schuldnerberatungsstelle); asset/means-test complexity or contested filings escalate harder than tate's referral line"}
  {:gate/id "G8" :gate/name "kotoba-eavt-audit" :gate/req "registries (ground) and plans (transient, :bond/is-transient — computed on read) on the append-only Datom log; a stale plan cannot outlive the registry entry it was computed from"}
  {:gate/id "G9" :gate/name "murakumo-only" :gate/req "any LLM step (situation classification, future waves) via LiteLLM 127.0.0.1:4000 only (ADR-2605215000); R0 ships zero LLM calls — pure registry lookup"}
  {:gate/id "G10" :gate/name "jurisdiction-honesty" :gate/req "procedures NEVER cross jurisdictions (structural filter in classify); an uncovered jurisdiction degrades to :unknown-jurisdiction — saisei NEVER guesses foreign insolvency law; coverage_report.cljc reports covered/193 + named gaps"}]

 :actor/non-goals
 [{:ng/id "N1" :ng/req "NOT a law firm / no representation / no individualized legal or financial advice; generic registry shapes + statutory pointers only"}
  {:ng/id "N2" :ng/req "does not draft or file the petition on the member's behalf (UPL) — self-file only, mirrors tate/toritsugi's default posture"}
  {:ng/id "N3" :ng/req "does not adjudicate eligibility or promise a discharge outcome — G2/G6"}
  {:ng/id "N4" :ng/req "does not duplicate tate's :insolvency track (creditor-side response to a THIRD PARTY's insolvency notice stays with tate) or toritsugi's administrative filings (passport/registry/tax) — saisei owns exactly the debtor-initiated court/formal-insolvency-forum petition surface"}
  {:ng/id "N5" :ng/req "does not duplicate yobel (voluntary doctrinal release) or amnesty (legal-person institutional restructuring) — saisei is the formal, legally-binding natural-person procedure layer those two name as their fallback"}
  {:ng/id "N6" :ng/req "no evasion of lawful obligations: non-dischargeable debt categories (where the law defines them) are disclosed honestly, never planned around"}]

 :actor/cells ; langgraph→WASM; .solve() raises at R0 (scaffold follow-up, mirrors yobel/amnesty S0 precedent)
 [{:cell/id "situation_intake" :cell/kind :langgraph :cell/runtime :wasm :cell/node "reuben"}
  {:cell/id "eligibility_classify" :cell/kind :langgraph :cell/runtime :wasm :cell/node "simeon"}
  {:cell/id "filing_plan" :cell/kind :langgraph :cell/runtime :wasm :cell/node "judah"}
  {:cell/id "refer_route" :cell/kind :langgraph :cell/runtime :wasm :cell/node "zebulun"}]

 :actor/lex
 [{:lex/id "memberSituation"}  ; com.etzhayyim.apps.saisei.memberSituation (encrypted at R1+)
  {:lex/id "eligibilitySignal"} ; com.etzhayyim.apps.saisei.eligibilitySignal (transient)
  {:lex/id "filingPlan"}]}      ; com.etzhayyim.apps.saisei.filingPlan (dry-run; member self-files)
