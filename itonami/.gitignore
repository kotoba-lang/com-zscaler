# runtime append-only factory-ops ledger (content-addressed commit-DAG) + generated
# readouts — machine-local, resume-safe, regenerable from the committed seed; never committed.
data/persisted/
out/
