;; iryo 医療 — レセプト / 医療保険請求 engine manifest
;; The billing counterpart to the karute 電子カルテ (EMR): karute holds the encrypted
;; clinical record; iryo computes the レセプト (診療報酬請求) FROM it and emits the レセ電
;; (online-claim) stream + FHIR claim. Charter-clean inversion of a proprietary レセコン /
;; EHR-billing vendor (ORCA-proprietary / Epic / Cerner).
{:actor/id "iryo"
 :actor/glyph "医療"
 :actor/did "did:web:iryo.etzhayyim.com"
 :actor/role "Japan 国内 医療保険 レセプト計算 + レセ電(レセプト電算)生成 + FHIR claim engine — the member-principal billing substrate the karute EMR hands off to. Resolves 点数→円(1点=10円), 薬剤料 五捨五超四入, 一部負担金 端数処理, 高額療養費 自己負担限度額; emits IR/RE/HO/KO/SY/SI/IY/TO レセ電 records (PHI injected only at submission, never the public substrate)."
 :actor/tier "B"
 :actor/parent-adr "2605263000"   ;; iyashi L4 Care tier
 :actor/integrates ["karute" "iyashi" "yakushi" "toritate" "chigiri"]
 :actor/cells
 [{:cell/id "rezept"         :cell/kind :langgraph :cell/runtime :wasm}
  {:cell/id "receden"        :cell/kind :langgraph :cell/runtime :wasm}
  {:cell/id "validate"       :cell/kind :langgraph :cell/runtime :wasm}
  {:cell/id "ingest-billing" :cell/kind :langgraph :cell/runtime :wasm}]
 :actor/gates
 {"G1" "member-principal — the licensed 保険医療機関 is the billing PRINCIPAL; iryo is open substrate only, never originates a claim on its own key (no-server-key)"
  "G2" "PHI-encrypted — 氏名/生年月日/SOAP free-text are PHI; they enter the レセ電 stream only via the operator decrypt callback at submission, never the public substrate (ADR-2605181100)"
  "G3" "no-server-key — online 請求(送信) is operator-gated; iryo computes + drafts only (default :draft)"
  "G4" "master-honest — points resolved through a loaded 厚労省 master; the bundled seed is representative; the engine never hard-codes a tariff value"
  "G5" "non-adjudicating — validation EMITS discrepancy observations; the 審査支払機関(基金/国保連) + clinic decide; iryo approves/denies nothing"
  "G6" "Murakumo-only inference (kotoba llm host binding; no external LLM)"
  "G7" "no insurance-billing payroll/inflow to the religious-corp — iryo is a TOOL the member clinic self-operates (iyashi G13 boundary preserved)"}}
