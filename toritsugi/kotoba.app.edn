;; kotoba.app.edn — actor:toritsugi KOTOBA Mesh application manifest (service concierge on-http, SERVICE-MESH-PATTERN.md).
{:kotoba.app/name    "toritsugi"
 :kotoba.app/version "0.1.0"
 :kotoba.app/components
 [{:name "toritsugi" :src "methods/mesh.clj" :scale 1
   :triggers [{:type :http :route "/toritsugi"}] :requires #{:cap/kqe}}
  ;; self-publication: the actor periodically drafts its OWN history (source-cited
  ;; aggregate guidance/relay records) + procedures (the coded government procedures it
  ;; guides — 名称/所管/必要書類/根拠法令/self-submit steps) as DRY-RUN social posts, and —
  ;; once it holds its own mesh-runtime key (self-custodied, no-server-key, ADR-2605231525)
  ;; under a member CACAO leash — signs + emits them itself. Live broadcast stays Council
  ;; Lv6+ + operator gated (§1.12/G11; mirrors toritsugi_submit's G15 代行-is-gated);
  ;; R0 = dry-run. ADR-2606272355.
  {:name     "toritsugi-social"
   :src      "methods/social.cljc"
   :scale    1
   :triggers [{:type :tick :cron "0 */6 * * *"}                 ; every 6h: self-publish backlog
              {:type :kse  :topic "etzhayyim/actor/toritsugi/publish"}]
   :requires #{:cap/kqe :cap/atproto}}]
 :kotoba.app/placement {:spread :zone :require {:tier "edge"}}}
