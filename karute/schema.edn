;; 20-actors/karute/schema.edn — Datomic/Datascript 互換スキーマ定義（自動生成, karute actor.edn 単発変換）
;; :db/ident 属性定義のリスト。Datomic 固有キー(:db.install/_attribute 等)は使わない。
;; 手編集禁止 — 再生成すると上書きされる。

[#:db{:ident :karute-deploy/actor, :valueType :db.type/string, :cardinality :db.cardinality/one} #:db{:ident :karute-deploy/stages, :valueType :db.type/string, :cardinality :db.cardinality/one}]
