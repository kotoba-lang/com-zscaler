;; shukubo 宿坊 — actor manifest
;; Pilgrim-lodging commons (Airbnb/Hotels charter-clean inversion), three concentric rings
;; ADR-2606071600
{:actor/id        "shukubo"
 :actor/glyph     "宿坊"
 :actor/tier      :tier-b
 :actor/integrates ["musubi" "okaimono" "todoke" "wakai" "warifu"]
 :actor/domain    "shukubo.etzhayyim.com"
 :actor/purpose   "Airbnb/Hotels-inverse lodging commons — Ring 0 hospitality-first (covenantal/pilgrim/mutual-aid stays, cash≡0 or cost-share) → Ring 1 internal SBT↔SBT stays (member/actor-hosted, USDC+TitheRouter, ZERO platform commission, warifu, member-signed) → Ring 2 external lodging mirror (data-only discovery; member self-books on operator's own site; no inflow). No commission, no surge, no discriminatory scoring, Murakumo-only, kotoba-EAVT-native."
 :actor/status    :r1
 :actor/adr       "2606071600"

 :actor/charter-rider
 {:rider/version "2.0"
  :rider/applied true}

 :actor/gates
 [{:gate/id "G1"  :gate/name "consent-bound"             :gate/rule "member DID-signed consent before stay-capture, booking, or any host/guest action"}
  {:gate/id "G2"  :gate/name "value-inflow-boundary"     :gate/rule "ZERO platform commission ever; Ring-1 settlement internal SBT↔SBT only; Ring-2 booking = member transacts directly with lodging operator (shukubo never merchant-of-record); no commission field representable"}
  {:gate/id "G3"  :gate/name "no-ads-no-commission"      :gate/rule "listings DATA-ONLY; no sponsored ranking / paid placement; no per-stay extraction (Charter Rider §2)"}
  {:gate/id "G4"  :gate/name "wellbecoming-anti-dark"    :gate/rule "no urgency/scarcity/FOMO ('1 room left'); commons + cost-share ordering; optimize covenantal hospitality, never booking conversion"}
  {:gate/id "G5"  :gate/name "murakumo-only"             :gate/rule "NL stay-need parsing / semantic match via KotobaLLM 127.0.0.1:4000; no external LLM (ADR-2605215000)"}
  {:gate/id "G6"  :gate/name "kotoba-eavt-native"        :gate/rule "stay/booking/host/settlement are kotoba Datoms; no RisingWave/SQL/Lance as canonical"}
  {:gate/id "G7"  :gate/name "tithe-non-fiat"            :gate/rule "Ring-1 settlement USDC Base L2 + ERC-4337 + warifu only; TitheRouter 10% auto-split; no Stripe/fiat"}
  {:gate/id "G8"  :gate/name "no-server-key"             :gate/rule "member/host signs each payment with own passkey/ERC-4337 smart-account; shukubo holds no card secret/key (ADR-2605231525); server signature refused"}
  {:gate/id "G9"  :gate/name "pii-encrypted-envelope"    :gate/rule "stay records + guest identity → com.etzhayyim.encrypted.*, DID-bound (ADR-2605181100)"}
  {:gate/id "G10" :gate/name "sourcing-legality"         :gate/rule "Ring-2 mirror: public availability only, robots.txt+ToS+rate-limit honored; :representative honesty; no fabricated availability/price"}
  {:gate/id "G11" :gate/name "outward-gated"             :gate/rule "live external OTA ingest + real external booking = Council Lv7+ + operator"}
  {:gate/id "G12" :gate/name "hospitality-dignity"       :gate/rule "no discriminatory host/guest scoring; pilgrim-welcome default; persons never scored; only habitability+safety of the SPACE is attested"}
  {:gate/id "G13" :gate/name "no-surge"                  :gate/rule "price flat or cost-share; demand never raises price; no dynamic/demand pricing field representable"}
  {:gate/id "G14" :gate/name "privacy-by-construction"   :gate/rule "no in-stay surveillance (cameras/biometrics) as a listing feature; guest privacy a habitability invariant"}]

 :actor/cells
 [{:cell/id "listing"  :cell/kind :datalog   :cell/runtime :kotoba :cell/doc "stay/host registry across rings"}
  {:cell/id "discover" :cell/kind :langgraph :cell/runtime :wasm   :cell/doc "stay-need → ranked stays (commons-first, Murakumo)"}
  {:cell/id "book"     :cell/kind :langgraph :cell/runtime :wasm   :cell/doc "consent → reservation → (Ring1) member-signed settle | (Ring2) self-book handoff"}
  {:cell/id "settle"   :cell/kind :datalog   :cell/runtime :kotoba :cell/doc "Ring-1 USDC + TitheRouter via warifu (intent-only at R0)"}]

 :actor/lexicons
 ["com.etzhayyim.shukubo.stay"
  "com.etzhayyim.shukubo.booking"
  "com.etzhayyim.shukubo.host"
  "com.etzhayyim.shukubo.settlement"]

 :actor/siblings
 {:okaimono "goods provisioning commons (same three-ring shape)"
  :musubi   "covenant/hospitality vow (Ring-0 pilgrim stays)"
  :wakai    "mutual-aid funded Ring-0 shelter"
  :warifu   "Ring-1 zero-fee settlement rails"
  :todoke   "logistics sibling"}}
