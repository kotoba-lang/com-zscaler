;; sentei — actor manifest (converted from manifest.jsonld, jsonld-retirement wave)
{:actor/id "sentei"
 :actor/glyph "剪定"
 :actor/display-name "剪定 — Council as Pruner (post-hoc pruning governance)"
 :actor/tier :tier-b
 :actor/purpose "Council as PRUNER (剪定者), not censor. The organism grows from its root unstoppably; branches manifest on the append-only Datom log; sentei prunes the overgrown / charter-violating ones AFTER they manifest, to keep the organism beautiful (美しく保つ). Re-times every outward gate (G7/G11/'Council Lv6+ BEFORE live') from prior restraint to a pruning target: actors self-publish; sentei cuts back afterward — transparently, signed, voted, and reversibly (regraft heals a mistaken cut; the pruned branch's history is never deleted — 非終末論). More faithful than prior restraint to non-eschatology (append-only has no halt; the only real enforcement is append-a-retraction) and Transparent Force (a prune is a logged/signed/public act over a thing that already exists). ZERO invariant amendments. Per ADR-2606072000."
 :actor/adr ["2606072000" "2606052100" "2605231525" "2605312345" "2605192100"]
 :actor/gates [#:gate{:id "G1", :rule "no-prior-restraint — prune ONLY a manifested branch (branchManifested const true)"} #:gate{:id "G2", :rule "append-only / 非終末論 — a prune appends; history survives, as-of-recoverable"} #:gate{:id "G3", :rule "growth-unstoppable — no halt-organism action"} #:gate{:id "G4", :rule "Transparent Force — on-chain + Council Lv6+/Lv7+ + 1 SBT=1 vote if contested"} #:gate{:id "G5", :rule "no-server-key — Council/member-signed (serverHeldKey const false)"} #:gate{:id "G6", :rule "reversible — every prune has the inverse regraft"} #:gate{:id "G7", :rule "care-telos 美しく保つ — basis required, nonAdjudicating const true, no verdict value"} #:gate{:id "G8", :rule "Murakumo-only — overgrowth classification via Murakumo; model flags, Council decides"}]}
