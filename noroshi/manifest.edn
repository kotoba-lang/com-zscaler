{:actor/id        "noroshi"
 :actor/glyph     "烽"
 :actor/tier      :tier-b
 :actor/integrates ["silicon" "tsukuru" "tsutae" "watatsuna"]
 :actor/domain    "noroshi.etzhayyim.com"
 :actor/did       "did:web:etzhayyim.com:actor:noroshi"
 :actor/purpose   "Photonics-electronics convergence (光電融合) communication-chip actor — the silicon-photonic / co-packaged-optics (CPO) sibling of the electronic silicon/iwakura/fuigo line and the RF tsutae comms device. 烽 (狼煙, beacon-fire) is the original optical telecom: a watchtower SENSES a distant fire and RELAYS a coded message — one emission, two functions, which is exactly ISAC (Integrated Sensing And Communication). Three faces: (chip) design + link-budget of photonic-IC / CPO communication chips on open photonic-EDA; (isac) an OFDM-JCAS simulator that does communication capacity AND range-Doppler sensing from one waveform; (packaging) photonic assembly robotics (fibre↔grating active alignment, photonic wire-bonding) with a laser-safety interlock. CIVILIAN-only by construction: weaponisation (directed-energy / laser-dazzle / fire-control radar) is structurally unrepresentable (N1), ISAC senses objects never persons (N2), and EPDA is clean-room open-source only (no Cadence/Synopsys/Lumerical/Ansys; the iwakura yosys/OpenLane/sky130 precedent extended to photonics)."
 :actor/status    :r0
 :actor/adr       "2606051600"
 :actor/related   ["2605242500" "2605242515" "2605242530" "2605261300" "2606012600" "2606032100" "2606032130" "2606033600" "2606036000" "2605261800" "2606010600" "2605231525" "2605215000" "2605241900" "2605312345" "2606042330" "2605192100"]
 :actor/siblings  ["silicon" "iwakura" "fuigo" "tsutae" "watatsuna" "tsukuru"]
 :actor/labor
 {:isic   ["C26" "C2620" "J61"]
  :isco   ["2152" "7311" "8212"]
  :unspsc ["32" "41" "43"]
  :lps-rank nil
  :note "design + simulation + assembly of optical/photonic communication components; packaging robotics displaces fibre-alignment technicians → G2 dividend-coupled"}

 :actor/charter-rider {:rider/version "2.0" :rider/applied true}

 ;; Clean-room photonic-EDA tiers — open-source EPDA + open standards ONLY (G1). No proprietary EDA/PDK.
 :actor/epda-tiers
 [{:tier/id "E1" :tier/kind :open-photonic-eda  :tier/preference 1 :tier/note "GDSFactory (layout/PIC) + Meep/MPB (FDTD/mode, MIT) + KLayout (GDSII) — published-API photonic design, like sumitsubo's CAD kernel"}
  {:tier/id "E2" :tier/kind :open-digital-eda   :tier/preference 2 :tier/note "Verilator / yosys / OpenLane2 + an open PDK (sky130-class) for the electronic half — the verified iwakura/fuigo RTL→GDSII flow (ADR-2606012800)"}
  {:tier/id "E3" :tier/kind :open-standard-wire :tier/preference 3 :tier/note "GDSII / OASIS layout, Touchstone S-parameters, IEC 60825 laser classes, Telcordia GR-468 qual SHAPES as neutral vocabulary; vendor PDK/EDA names in docstrings only (nv-compat pattern)"}]

 ;; Force class on every photonicDevice / isacWaveform. :weaponizable is structurally unrepresentable (N1).
 :actor/force-classes [:civilian-comms :civilian-sensing :fab-process-laser]

 :actor/gates
 [{:gate/id "G1" :gate/name "cleanroom-epda" :gate/req "open-source photonic + digital EDA ONLY (GDSFactory / Meep / KLayout / Verilator / yosys / OpenLane + open PDK); NO proprietary EDA/PDK bundling (Cadence/Synopsys/Lumerical/Ansys), no NDA foundry PDK in-repo, no decompile/trademark/fork (sumitsubo G1 + iwakura open-EDA precedent); vendor names in docstrings only"}
  {:gate/id "G2" :gate/name "displacement-dividend-coupling" :gate/req "packaging robotics that displaces human fibre-alignment / photonic-assembly technicians LIVE requires a funded tenure-weighted cohort registered for the Displacement Dividend (ADR-2606032130); DEFINING gate of the robotics wave"}
  {:gate/id "G3" :gate/name "civilian-force-separation" :gate/req "optical power + ISAC sensing are CIVILIAN-only; weaponisation (directed-energy weapon / laser dazzler / fire-control & targeting radar) is structurally unrepresentable in schema, lexicon, and methods (Mission Charter §1.12 force-separation; the iwakura/nusa :class precedent)"}
  {:gate/id "G4" :gate/name "sensing-not-surveillance" :gate/req "an ISAC senseEstimate is an OBJECT's range + radial velocity, NEVER a person / biometric / through-wall person-imaging / pattern-of-life (watari G4); no person field is representable"}
  {:gate/id "G5" :gate/name "laser-safety-soft" :gate/req "the IEC 60825 enclosure-interlock + class gate is BEST-EFFORT soft-safety, NOT a certified safety controller; certified IEC 60825 / IEC 61508 hard-RT live actuation = R5/Lv7+ (kotoba-os N2 precedent)"}
  {:gate/id "G6" :gate/name "murakumo-only" :gate/req "any LLM-assisted device/layout generation or design narration via LiteLLM 127.0.0.1:4000 / edge Murakumo only (ADR-2605215000); no commercial GPU rental / cloud-EDA in the religious-corp path"}
  {:gate/id "G7" :gate/name "no-server-key" :gate/req "a tapeout / mask-set order / live packaging-robot actuation command is member/operator-signed; the platform NEVER holds the signing key (ADR-2605231525); serverHeldKey=false on every job"}
  {:gate/id "G8" :gate/name "outward-gated" :gate/req "live foundry tapeout / mask set / device measurement / live laser energising / live packaging actuation is Council Lv6+ + operator gated (Lv7+ for Class-3B/4 lasers near humans); R0-R2 = design + simulation only"}
  {:gate/id "G9" :gate/name "kotoba-eavt-audit" :gate/req "every device, link budget, waveform, sense estimate, packaging job = Datoms (as-of, replayable; 非終末論); canonical state in the kotoba Datom log, never RisingWave (ADR-2605312345)"}
  {:gate/id "G10" :gate/name "sourcing-honesty" :gate/req ":representative device parameters + fleet; honest staging — NO silicon exists, sims are arithmetic/DSP not measured devices, ISAC aliases beyond R_max / the Doppler interval"}
  {:gate/id "G11" :gate/name "sbom-provenance" :gate/req "a fabricated photonic die carries a CycloneDX SBOM into kotoba (wasm-sbom / giemon part-graph precedent, ADR-2606036000 / 2605312330); gated at tapeout (G8)"}]

 :actor/non-goals
 [{:ng/id "N1" :ng/req "no weaponisation — no directed-energy weapon, no laser dazzler, no fire-control / targeting radar; the weaponizable force class is structurally unrepresentable (Mission Charter §1.12)"}
  {:ng/id "N2" :ng/req "no person-surveillance — ISAC senses objects (range/velocity), never persons; no biometric, no through-wall person-imaging, no pattern-of-life (watari N-class)"}
  {:ng/id "N3" :ng/req "not a certified safety controller — soft laser-safety / soft-RT interlock only; certified IEC 60825 / IEC 61508 = R5/Lv7+ (kotoba-os N2)"}
  {:ng/id "N4" :ng/req "no fabricated coverage — sims report honest limits (aliasing, no measured device); not a measurement / characterisation lab at R0"}
  {:ng/id "N5" :ng/req "no proprietary EDA / NDA PDK — no Cadence/Synopsys/Lumerical/Ansys bundling, no decompile/trademark/fork, no NDA foundry PDK committed (clean-room, sumitsubo N-class)"}
  {:ng/id "N6" :ng/req "no commercial GPU rental / cloud-EDA in the religious-corp inference/design path (Murakumo-only, ADR-2605215000)"}
  {:ng/id "N7" :ng/req "not a foundry and not a tapeout broker at R0 — a design + simulation substrate; live fab / mask set is Council Lv6+ + operator gated (G8)"}
  {:ng/id "N8" :ng/req "no cash payment for packaging labour or demonstrations (no-payroll, cash=0; contribution → vocation → donation only)"}]

 :actor/cells
 [{:cell/id "device_design"    :cell/kind :langgraph :cell/runtime :wasm :cell/node "naphtali" :cell/face :chip :cell/coded true}
  {:cell/id "link_budget"      :cell/kind :langgraph :cell/runtime :wasm :cell/node "gad"      :cell/face :chip}
  {:cell/id "isac_waveform"    :cell/kind :langgraph :cell/runtime :wasm :cell/node "asher"    :cell/face :isac}
  {:cell/id "sense_estimate"   :cell/kind :langgraph :cell/runtime :wasm :cell/node "benjamin" :cell/face :isac}
  {:cell/id "active_alignment" :cell/kind :langgraph :cell/runtime :wasm :cell/node "joseph"   :cell/face :packaging :cell/coded true}
  {:cell/id "reliability_qual" :cell/kind :langgraph :cell/runtime :wasm :cell/node "manasseh" :cell/face :packaging :cell/coded true}]

 :actor/lex
 [{:lex/id "photonicDevice"}
  {:lex/id "opticalLinkBudget"}
  {:lex/id "isacWaveform"}
  {:lex/id "senseEstimate"}
  {:lex/id "packagingJob"}]}
