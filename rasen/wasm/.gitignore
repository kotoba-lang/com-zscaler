# cljc-native build artifacts (ADR-2606261200): cherry/esbuild/jco outputs + the
# componentized wasm. The operator builds via `bb -m rasen.tools.build`; never committed.
build/
rasen-actor.wasm
transpiled/
*.mjs
*.bundle.js
app.shimmed.js
node_modules/
# legacy componentize-py leftovers (removed in the cljc-native migration)
_embedded_seed.py
*.py
