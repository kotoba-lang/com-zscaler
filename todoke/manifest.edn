;; todoke 届け — actor manifest (Gen-3 kotoba-native)
;; Last-mile ("one-mile") autonomous delivery — curb-to-door, small payload, SAE L4 ceiling.
;; ADR-2606042300 (R0 scaffold). Sibling of wadachi (轍, inter-site ground autonomy).
{:actor/id        "todoke"
 :actor/glyph     "届け"
 :actor/tier      :tier-b
 :actor/integrates ["haraedo" "okaimono" "tazuna" "wadachi"]
 :actor/domain    "todoke.etzhayyim.com"
 :actor/did       "did:web:etzhayyim.com:actor:todoke"
 :actor/purpose   "Last-mile ('one-mile') autonomous delivery — curb-to-door small-payload transport that closes the gap between wadachi inter-site ground autonomy and the recipient's door. Consumes the kami-autodrive GNC crate; the no-gig delivery limb of okaimono's provisioning commons; couples to the courier Displacement Dividend."
 :actor/status    :r0
 :actor/adr       "2606042300"
 :actor/related   ["2606010600" "2605242000" "2606013100" "2606012100" "2606010200" "2606032100" "2606032130" "2606042100" "2605215000" "2605231525"]
 :actor/labor
 {:isic   "H53"
  :isco   ["9621" "8322"]
  :unspsc ["78"]}

 :actor/charter-rider {:rider/version "2.0" :rider/applied true}

 :actor/fleet
 [{:robot/id "hakobi" :robot/glyph "運び" :robot/role "sidewalk last-mile delivery rover (curb-to-door, <=25 kg)"}
  {:robot/id "tobira" :robot/glyph "扉"   :robot/role "smart parcel-locker dock (curb hand-off / pickup)"}
  {:robot/id "meyasu" :robot/glyph "目安" :robot/role "perception + telemetry pod (on-device only, G8)"}
  {:robot/id "tedori" :robot/glyph "手取" :robot/role "load/unload manipulator (curb <-> rover bay)"}]

 :actor/gates
 [{:gate/id "G1"  :gate/name "open-source-firmware"           :gate/req "all platform/control code Apache 2.0 (Rust/WASM); no proprietary vehicle-control IP"}
  {:gate/id "G2"  :gate/name "displacement-dividend-coupling" :gate/req "no live courier displacement without the cohort registered for the tenure-weighted Displacement Dividend (ADR-2606032130)"}
  {:gate/id "G3"  :gate/name "witness-quorum"                 :gate/req ">=1 safety-monitor sig + recipient consent reference required to finalize a mission"}
  {:gate/id "G4"  :gate/name "murakumo-only"                  :gate/req "inference via LiteLLM 127.0.0.1:4000 only (ADR-2605215000)"}
  {:gate/id "G5"  :gate/name "no-gig-cash-zero"               :gate/req "no gig/piece-rate courier model; no payroll; cash to adherents ≡0 (the inversion of gig-courier exploitation; okaimono no-gig)"}
  {:gate/id "G6"  :gate/name "wellbecoming"                   :gate/req "remove courier toil, never intensify it; no dark-pattern dispatch"}
  {:gate/id "G7"  :gate/name "sae-l4-sidewalk-envelope"       :gate/req "SAE J3016 ceiling = 4 (N2); commanded speed <= per-zone cap (sidewalk 1.8 / bike-lane 4.2 m/s); vehicular roads outside ODD — enforced by the todoke-route safety envelope (refusal, not clamp)"}
  {:gate/id "G8"  :gate/name "privacy-by-construction"        :gate/req "proof-of-delivery ON-DEVICE only — NO cloud imagery / facial recognition / biometric recipient ID (admissible: signature / locker-code / on-device photo-hash)"}
  {:gate/id "G9"  :gate/name "outward-gated"                  :gate/req "live operation Council Lv6+ + operator gated; R0-R1 design+sim only; live actuation Transparent-Force bound (every command an on-chain Datom)"}
  {:gate/id "G10" :gate/name "sourcing-honesty"               :gate/req ":representative seed flagged; no inflated capability"}
  {:gate/id "G11" :gate/name "kotoba-eavt-native"             :gate/req "state is kotoba EAVT Datoms (job/route/run/handoff/mission); no SQL/RisingWave/Cypher"}
  {:gate/id "G12" :gate/name "no-server-key"                  :gate/req "platform holds no key; recipient/member signs hand-off + settlement; server signature refused (ADR-2605231525)"}
  {:gate/id "G13" :gate/name "consent-bound"                  :gate/req "no doorstep hand-off without a recorded encrypted recipient consent reference"}
  {:gate/id "G14" :gate/name "charter-rider"                  :gate/req "Charter Rider §2(a-h) scanned; no armed/weaponized platform or payload; no contraband payload class"}
  {:gate/id "G15" :gate/name "tithe-non-fiat"                 :gate/req "payments USDC Base L2 + ERC-4337 + TitheRouter 10% (no Stripe/fiat)"}]

 :actor/non-goals
 [{:ng/id "N1" :ng/req "no military/weaponized/surveillance-payload delivery"}
  {:ng/id "N2" :ng/req "no SAE Level 5; no high-speed vehicular-road autonomy (that is wadachi R2+)"}
  {:ng/id "N3" :ng/req "no regulated/contraband payloads (pharma→yakushi cold-chain; hazmat; age-restricted)"}
  {:ng/id "N4" :ng/req "no gig-labour / piece-rate courier model"}
  {:ng/id "N5" :ng/req "no cloud-side imagery / facial-recognition recipient identification"}
  {:ng/id "N6" :ng/req "no aerial drone delivery at R0 (ground/sidewalk first; aerial = kami-autodrive Drone class, separately gated)"}]

 :actor/cells
 [{:cell/id "parcel_intake"     :cell/kind :langgraph :cell/runtime :wasm :cell/node "naphtali"}
  {:cell/id "route_sequencing"  :cell/kind :langgraph :cell/runtime :wasm :cell/node "joseph"}
  {:cell/id "autonomous_run"    :cell/kind :langgraph :cell/runtime :wasm :cell/node "judah"}
  {:cell/id "handoff_proof"     :cell/kind :langgraph :cell/runtime :wasm :cell/node "simeon"}
  {:cell/id "telemetry_log"     :cell/kind :langgraph :cell/runtime :wasm :cell/node "levi"}]

 :actor/lex
 [{:lex/id "deliveryJob"}
  {:lex/id "lastMileRoute"}
  {:lex/id "handoffProof"}
  {:lex/id "missionCompleteRecord"}
  {:lex/id "safetyAlert"}]

 :actor/rust
 {:crate "route/"
  :name "todoke-route"
  :doc "Pure, zero-dep Rust core: last-mile stop sequencing (NN + 2-opt) + SAE-L4 sidewalk ODD safety envelope (G7) + R1 curb-to-door rover sim (kinematic stand-in for kami-autodrive). 13 tests green (7 route + 6 sim) + example curb_to_door. Sequencer mirrored in Python methods/last_mile.py (parity-tested)."
  :modules ["lib.rs (sequencing + G7 envelope)" "sim.rs (R1 curb-to-door drive)"]
  :examples ["curb_to_door"]}}
