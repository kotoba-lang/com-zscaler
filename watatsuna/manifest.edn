;; watatsuna — actor manifest (converted from manifest.jsonld, jsonld-retirement wave)
{:actor/glyph "綿津綱"
 :actor/display-name "綿津綱 (わたつな)"
 :actor/cells [{"id" "cell:watatsuna.ingest", "method" "methods/ingest.py", "input" "data/ingest/*.json (submarinecablemap/TeleGeography-shaped public dataset)", "output" ["data/ingest/telegeography-bridge.kotoba.edn" "data/cable-graph.merged.kotoba.edn"], "pipeline" "public dataset → normalize to :cable/:station/:cable.link → dedup-merge with seed (seed wins)", "gate" "G7 — live --live fetch requires WATATSUNA_OPERATOR_GATE (Council); offline/sample mode default, tagged :representative (G5)"} {"id" "cell:watatsuna.analyze", "method" "methods/analyze.py", "input" "data/cable-graph.merged.kotoba.edn (or data/seed-cable-graph.kotoba.edn)", "output" ["out/intel-report.md" "out/cable-criticality.kotoba.edn"], "pipeline" "classify → cable⇄station incidence → station degree/capacity → chokepoint-load → cable-diversity → redundancy-gap (aggregate-first)"} {"id" "cell:watatsuna.plan", "method" "methods/plan.py", "input" "data/cable-graph.merged.kotoba.edn (or seed)", "output" ["out/resilience-plan.md" "out/resilience-plan.kotoba.edn"], "pipeline" "resilience → watatsumi mission plan: lay-diverse-route (redundancy gaps) · pre-stage-repair (high chokepoint-load) · monitor (brittle cables). G2/N8 — redundancy+repair+monitor ONLY, no interdiction output by construction.", "consumedBy" "actor:watatsumi cable-laying fleet (data/cable-laying-fleet.kotoba.edn)"} {"id" "cell:watatsuna.viz", "method" "viz/build_viz_data.py", "input" "data/cable-graph.merged.kotoba.edn (or seed)", "output" ["viz/cable-resilience.json" "viz/cable-resilience.htm" "viz/cable-globe.htm"], "pipeline" "analyze → payload → self-contained canvas 2D map + 3D orthographic globe (G2: redundancy surface, not target-list). kami-engine WASM globe integration (shared w/ kanae/shibuya) deferred; cable-resilience.json is the contract."}]
 :actor/gates [#:gate{:id "G1", :rule "public infrastructure only — cable systems/owners/landings/capacity are public record; classified routes, precise armoring depth, live repair-vessel position beyond public AIS = OUT OF SCOPE"} #:gate{:id "G2", :rule "resilience-not-interdiction — chokepoint ranking routed to redundancy + repair; NEVER a target-list (watatsumi N8 + Charter Rider §2(d))"} #:gate{:id "G3", :rule "aggregate-first + claimed-first output"} #:gate{:id "G4", :rule "no intent adjudication — fault :kind mirrors only the public bulletin's own classification; sabotage is a state matter, never asserted by watatsuna"} #:gate{:id "G5", :rule "sourcing honesty (:authoritative | :representative | :synthesized) on every node/edge; no fabricated coverage"} #:gate{:id "G6", :rule "Murakumo-only narration (ADR-2605215000)"} #:gate{:id "G7", :rule "outward-gated — live planet-scale ingest (TeleGeography feed / cable-ship AIS) = Council + operator"} #:gate{:id "G8", :rule "no git-lfs; large geo assets DataLad → IPFS (80-data/submarine-cable)"} #:gate{:id "G9", :rule "no PII — infrastructure graph only; any incidental personal data → encrypted envelope, excluded by default"}]
 :actor/adr "2606012600"
 :actor/tier :tier-b
 :actor/integrates ["kanae" "watatsumi"]
 :actor/purpose "World submarine-cable network knowledge graph — datafies cable SYSTEMS, LANDING STATIONS, SEGMENTS and FAULT bulletins into the kotoba Datom log; surfaces chokepoint / single-point-of-failure concentration routed to redundancy + faster repair. A resilience map, never a target-list."
 :actor/lexicons ["com.etzhayyim.cable.registerCableSystem" "com.etzhayyim.cable.registerLandingStation" "com.etzhayyim.cable.registerSegment" "com.etzhayyim.cable.flagCableFault"]
 :actor/status :r0-design-only
 :actor/related ["2606011000" "2606011500" "2606011800" "2605262130" "2605312345" "2605241500" "2605252200"]
 :actor/id "watatsuna"}
