[project]
name = "etzhayyim-organism"
version = "0.1.0"
description = "Religious-corp artificial organism — active-inference tick daemon (ADR-2605192100 prior, _observations/ posterior)"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
etzhayyim-organism = "etzhayyim_organism.__main__:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/etzhayyim_organism"]
