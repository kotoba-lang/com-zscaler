# cljc-native build artifacts (ADR-2606261200): cherry/esbuild/jco outputs + the
# componentized wasm. The operator builds these via `bb aburi:build-wasm`; never committed.
build/
aburi-actor.wasm
transpiled/
*.mjs
*.bundle.js
app.shimmed.js
node_modules/
# legacy componentize-py leftovers (removed in the cljc-native migration)
_seed.py
*.py
