{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashka"
               :extra-paths ["test"]
               :requires ([zap_energy.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'zap_energy.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
