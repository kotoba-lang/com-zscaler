{:actor/id        "chie"
 :actor/glyph     "智慧"
 :actor/tier      :tier-b
 :actor/integrates ["abaki" "handotai" "kabuto" "kanjo" "kasa" "keizu" "kenkyusha" "kosatsu" "tsumugi"]
 :actor/domain    "chie.etzhayyim.com"
 :actor/did       "did:web:etzhayyim.com:actor:chie"
 :actor/purpose   "AI-ecosystem knowledge-graph mirror — the AI sibling of the power-mirror lineage (tsumugi/keizu/kabuto/kanjō/kosatsu). Weaves AI labs / companies / research / standards bodies / funders / states / public-role persons / investment rounds / policy instruments / models and the 縁 between them (invests-in / compute-deal / talent-flow / governs / sets-standard / partners / holds-role / depends-on) into the kotoba Datom log, and runs an edge-primary 取-concentration pass over four axes (compute / capital / talent / policy) routed to OPENING (open-weights / open-compute / anti-monopoly). map-not-target, non-adjudicating, edge-primary, never-trades. clj-native (.cljc), kotoba/Datom-native."
 :actor/status    :r1
 :actor/adr       "2606171200"
 :actor/related   ["2606011800" "2605262130" "2605312345" "2605081300" "2606073000"
                   "2606066001" "2606022000" "2606032000" "2606131500" "2605215000"
                   "2606091000" "2605192415"]

 :actor/charter-rider {:rider/version "3.1" :rider/applied true}

 :actor/lens
 {:axes  [:compute :capital :talent :policy]
  :route :opening
  :unit  :en/grasping-load}                 ;; 取 lives only on edges (N1)

 :actor/gates
 [{:gate/id "G1" :gate/name "opening-map-not-winner-rank" :gate/req "opening-priority = Σ inbound accumulation × (1 − openness); an OPEN accumulator scores 0. No capability grade, no winner forecast, no investment advice (N3). Map points at opening the concentration, never at a winner. Test-enforced."}
  {:gate/id "G2" :gate/name "edge-primary-public-role-only" :gate/req "取 lives ONLY on edges; node priority computed on read, no stored per-entity score. Persons appear ONLY as public-power ROLE nodes (:ai.role/person), never private profiles. verify.cljc rejects private-profile attrs."}
  {:gate/id "G3" :gate/name "non-adjudicating" :gate/req "funding rounds, designations, :ai/open? are DISCLOSED facts, never chie verdicts. :ai/open? flags the weights, not capability worth."}
  {:gate/id "G4" :gate/name "never-trades-never-forecasts" :gate/req ":trade / :forecast-point / :capability-grade / :winner-rank / :ai/score are UNREPRESENTABLE — undeclared in the schema, refused by the emitter, scanned by verify.cljc."}
  {:gate/id "G5" :gate/name "sourcing-honesty" :gate/req "every record carries :organism/sourcing :representative | :authoritative; seed is bounded; coverage of the world is ~0 by design; coverage_report names the gaps (no fabricated coverage)."}
  {:gate/id "G6" :gate/name "murakumo-only" :gate/req "all narration via the Murakumo fleet (LiteLLM 127.0.0.1:4000 / EVO-X2 LAN / per-node Ollama; ADR-2605215000); digest.cljc refuses any non-fleet endpoint."}
  {:gate/id "G7" :gate/name "outward-gated" :gate/req "live planet-scale ingest (regulator texts / disclosed rounds / Wikidata) requires Council + operator DID; ingest.cljc/ingest-live REFUSES without CHIE_INGEST_LIVE + operator DID (no-server-key)."}]

 :actor/non-goals
 [{:ng/id "N1" :ng/req "never grades model capability or ranks 'who will win'"}
  {:ng/id "N2" :ng/req "never trades / forecasts a point / gives investment advice"}
  {:ng/id "N3" :ng/req "not a private dossier on individuals (public roles only)"}
  {:ng/id "N4" :ng/req "does not duplicate sibling data — references kanjō/kabuto/handotai/kasa/kenkyusha/keizu/kosatsu/abaki"}]

 :actor/methods
 [{:method/id "analyze"          :method/file "methods/analyze.cljc"          :method/runtime :clj}
  {:method/id "datom-emit"       :method/file "methods/datom_emit.cljc"       :method/runtime :clj}
  {:method/id "coverage-report"  :method/file "methods/coverage_report.cljc"  :method/runtime :clj}
  {:method/id "autorun"          :method/file "methods/autorun.cljc"          :method/runtime :clj}
  {:method/id "digest"           :method/file "methods/digest.cljc"           :method/runtime :clj}
  {:method/id "ingest"           :method/file "methods/ingest.cljc"           :method/runtime :clj}
  {:method/id "query"            :method/file "methods/query.cljc"            :method/runtime :clj}
  {:method/id "verify"           :method/file "methods/verify.cljc"           :method/runtime :clj}
  {:method/id "kqe"              :method/file "methods/kqe.cljc"              :method/runtime :clj}]

 :actor/cells
 [{:cell/id "ChieHeartbeatCell" :cell/entry "chie.cell/fire" :cell/runtime :clj :cell/node "gad"
   :cell/trigger {:kind :cron :expr "37 * * * *"} :cell/healthz 13082}]

 :actor/schema   "kotoba/schema.edn"
 :actor/vocabulary "ai-ecosystem-ontology"      ;; 00-contracts/schemas/ai-ecosystem-ontology.kotoba.edn
 :actor/seed     "data/seed-ai-ecosystem.kotoba.edn"

 :actor/bridge
 {:financials :kanjo :supply :kabuto :silicon :handotai :compute-cap :kasa
  :research :kenkyusha :gov-power :keizu :designation :kosatsu :antitrust :abaki}

 :actor/tests
 ["chie.tests.test-analyze" "chie.tests.test-coverage" "chie.tests.test-schema"
  "chie.tests.test-cell" "chie.tests.test-verify"
  "chie.methods.test-datom-emit" "chie.methods.test-autorun" "chie.methods.test-digest"
  "chie.methods.test-ingest" "chie.methods.test-query" "chie.methods.test-kqe"]}
