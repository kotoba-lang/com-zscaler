{:actor/id        "yadori"
 :actor/glyph     "宿"
 :actor/tier      :tier-b
 :actor/integrates ["okaimono"]
 :actor/domain    "yadori.etzhayyim.com"
 :actor/did       "did:web:etzhayyim.com:actor:yadori"
 :actor/purpose   "DNS-availability check + domain acquisition (宿り = taking up dwelling). RDAP/EPP availability lookup + Murakumo-only name suggestion + member-principal acquisition via the Cloudflare-Registrar at-cost path (okaimono assisted-checkout, ADR-2606012100). The charter-clean inverse of a retail registrar: no fiat inflow, no markup/affiliate, no speculation. Closes the gap between minting an actor DID and the name actually existing."
 :actor/status    :r0
 :actor/adr       "2606038400"
 :actor/related   ["2606012100" "2605231525" "2605215000" "2605192115" "2605222330" "2605211757" "2606013800" "2605181100"]
 :actor/labor
 {:isic   "J6311"
  :isco   ["2521" "3513"]
  :unspsc ["81"]
  :lps-rank nil}

 :actor/charter-rider {:rider/version "2.0" :rider/applied true}

 :actor/registrar-default {:registrar/id "cloudflare" :registrar/pricing :at-cost :registrar/markup false}

 :actor/gates
 [{:gate/id "G1" :gate/name "read-only-availability" :gate/req "availability via RDAP / EPP <check> / public WHOIS only; rate-limited; NO third-party zone enumeration (no AXFR brute, no subdomain-enum-as-product)"}
  {:gate/id "G2" :gate/name "no-fiat-inflow-member-principal" :gate/req "registrar fees never paid from religious-corp funds or any fiat processor; acquisition runs through okaimono member-principal assisted-checkout (ADR-2606012100); §1.3 preserved without Lv7+ amendment; yadori is never the buyer-of-record"}
  {:gate/id "G3" :gate/name "cloudflare-registrar-default" :gate/req "Cloudflare Registrar (at-cost, no markup) is the default; GoDaddy / external fiat-markup registrars NOT recommended; any external registrar Council-gated"}
  {:gate/id "G4" :gate/name "murakumo-only" :gate/req "name/NL inference via LiteLLM 127.0.0.1:4000 only (ADR-2605215000)"}
  {:gate/id "G5" :gate/name "no-server-key" :gate/req "registrar/EPP credentials held by member-operator, never platform-held; registration authorization = member signature (ADR-2605231525)"}
  {:gate/id "G6" :gate/name "no-squatting" :gate/req "no cybersquatting / typosquatting / trademark infringement / brand impersonation / speculation / parking / drop-catch; every candidate clears a held-trademark + confusable screen and the Charter-Rider §2(a)-(h) scanner"}
  {:gate/id "G7" :gate/name "outward-gated" :gate/req "live registrar mutate (reserve/register/transfer) + live RDAP fetch Council Lv6+ + operator gated; R0 = offline availability + intent build only"}
  {:gate/id "G8" :gate/name "sourcing-honesty" :gate/req ":representative RDAP bootstrap + fixtures flagged; unsupported TLD degrades honestly to :unsupported-tld"}
  {:gate/id "G9" :gate/name "pii-consent" :gate/req "registrant contact data (WHOIS) consent-bound + encrypted (com.etzhayyim.encrypted.*, ADR-2605181100); WHOIS-privacy proxy default"}]

 :actor/non-goals
 [{:ng/id "N1" :ng/req "no domain speculation / investment / resale / parking-for-revenue"}
  {:ng/id "N2" :ng/req "no cybersquatting / typosquatting / trademark infringement / brand impersonation"}
  {:ng/id "N3" :ng/req "no bulk drop-catching / automated mass registration"}
  {:ng/id "N4" :ng/req "no third-party zone surveillance product"}
  {:ng/id "N5" :ng/req "no DNS provisioning for prohibited content or detection-evasion (fast-flux / DGA / phishing infra)"}]

 :actor/cells
 [{:cell/id "availability_check" :cell/kind :langgraph :cell/runtime :wasm :cell/node "dan"}
  {:cell/id "name_suggest"       :cell/kind :langgraph :cell/runtime :wasm :cell/node "naphtali"}
  {:cell/id "registrar_quote"    :cell/kind :langgraph :cell/runtime :wasm :cell/node "gad"}
  {:cell/id "reservation"        :cell/kind :langgraph :cell/runtime :wasm :cell/node "asher"}
  {:cell/id "dns_provision"      :cell/kind :langgraph :cell/runtime :wasm :cell/node "issachar"}]

 :actor/lex
 [{:lex/id "availabilityRecord"}
  {:lex/id "nameCandidate"}
  {:lex/id "registrarQuote"}
  {:lex/id "reservationIntent"}
  {:lex/id "zoneProvisionPlan"}]}
