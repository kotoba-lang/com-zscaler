# CHARTER-VIOLATION sidecar — actor-manifest.jsonld

This JSON-LD manifest contains a centralized-DB reference (RisingWave/Postgres/Kysely/Drizzle/Prisma).

Per CLAUDE.md substrate boundary: replace with AT MST + IPFS + Base L2 anchor schema.
