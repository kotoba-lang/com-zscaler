# himawari 向日葵 — langgraph WASM actor deps (ADR-2606021200)
# LLM access is Murakumo-only via the kotoba host `llm` binding (127.0.0.1:4000);
# no external LLM client is listed here by design (G5 / Charter Murakumo-only,
# ADR-2605215000).
langgraph>=0.2
typing_extensions>=4.0
