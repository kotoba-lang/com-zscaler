{:actor/id        "masago"
 :actor/glyph     "真砂"
 :actor/tier      :tier-b
 :actor/integrates ["hataori" "hikari" "hotaru" "inochi" "kiyome" "rasen" "sanae" "silicon" "tatekata"]
 :actor/domain    "masago.etzhayyim.com"
 :actor/did       "did:web:etzhayyim.com:actor:masago"
 :actor/purpose   "Open materials-discovery (公開材料) Knowledge-Graph mirror — the materials-science sibling of the public-scientific-data mirror lineage (rasen 螺旋 genetics / inochi 命 biosphere). Mirrors the PUBLIC open-materials commons — Meta FAIR Chemistry Open Materials 2024 (OMat24, CC-BY-4.0) + the Materials Project / OQMD / NOMAD / AFLOW / JARVIS family — into the kotoba Datom log: materials (formula + crystal structure), their constituent elements, the computed PROPERTIES they carry (DFT / MLIP), and the downstream APPLICATION classes they are candidates for. Runs an edge-primary discovery-evidence pass (a material's discovery-priority = the integral of its incident computed-property + application-candidacy 縁 weighted by disclosed provenance confidence) routed to RESEARCH (a candidate shortlist for human scientists), never to a make/buy decision. Charter-clean BY CONSTRUCTION: open-license sources only (G4 — non-open license refused); computed PROPERTIES + crystal STRUCTURES only — no synthesis route / precursor / enrichment field is representable, and weaponizable application classes are unrepresentable (G1, §1.12); MLIP/ML model execution deferred to R2+ on owned/donated compute (G6 — never commercial GPU rental). Closes the AI-driven materials-discovery coverage gap (was zero-coverage); feeds the silicon (iwakura/fuigo), energy/battery (hikari), construction (tatekata) and robotics (sanae/hataori/kiyome) actors."
 :actor/status    :r0
 :actor/adr       "2606151027"
 :actor/related   ["2606101000" "2606051200" "2606073000" "2605312345" "2605262400" "2605215000" "2605231525" "2605192100" "2605192200" "2605242500" "2605261100"]
 :actor/labor
 {:isic   "M7210"
  :isco   ["2113" "2114" "2149"]
  :unspsc ["41"]
  :lps-rank nil}

 :actor/charter-rider {:rider/version "3.1" :rider/applied true}

 :actor/gates
 [{:gate/id "G1" :gate/name "research-map-not-weapons-or-synthesis" :gate/req "RESEARCH map of computed PROPERTIES + crystal STRUCTURES only; a synthesis-route / precursor / enrichment / processing field is NOT representable (schema + lexicon enum + analyze.cljc screen ex-info), and weaponizable :application/class values (:weapon :energetic :explosive :propellant :warhead :fissile :enrichment) are unrepresentable; fabrication + force are structurally excluded (§1.12, mirrors hotaru no-fabrication)"}
  {:gate/id "G2" :gate/name "edge-primary" :gate/req "discovery evidence lives ONLY on :en/grasping-load × disclosed :en/confidence; material discovery-priority is the integral computed on read; no :material/score; the computed VALUE rides the edge as :en/value (DISCLOSED, never re-judged)"}
  {:gate/id "G3" :gate/name "non-adjudicating" :gate/req "property values + provenance (DFT/MLIP/experimental) are DISCLOSED source facts, never masago verdicts; the output is a candidate shortlist surfaced to humans, never a make/buy/trade decision; no :verdict route; masago never trades"}
  {:gate/id "G4" :gate/name "open-license-only" :gate/req "every :dataset-source carries an OPEN license (CC-BY / open); a non-open license is refused (analyze.cljc screen ex-info); no proprietary / vendor formulations"}
  {:gate/id "G5" :gate/name "sourcing-honesty" :gate/req "every record :authoritative | :representative; coverage of the full ~10^8 open-materials commons ~0 by design; render-coverage names thin/missing buckets (no fabricated coverage)"}
  {:gate/id "G6" :gate/name "murakumo-only / no-commercial-gpu" :gate/req "narration via LiteLLM 127.0.0.1:4000 (ADR-2605215000); MLIP/ML model EXECUTION (running OMat24 potentials) is GPU compute → owned/donated compute ONLY, never RunPod / cloud-GPU rental (Rider §2(i)); R0/R1 mirror DISCLOSED source values, no model execution; execution is R2+ Council-gated"}
  {:gate/id "G7" :gate/name "outward-gated / no-server-key" :gate/req "live ingest (Materials Project REST / OMat24 dumps) + IPFS pin + publish require Council + operator DID; the analyzer loop does no network I/O; serverHeldKey=false (ADR-2605231525); R0 = offline analyze + schema + seed only"}
  {:gate/id "G8" :gate/name "content-addressed-canonical-state" :gate/req "render-datoms is the canonical kotoba Datom log (EAVT ground :add + derived transient; ADR-2605312345); large dumps via DataLad → IPFS (ADR-2605262400), no git-lfs"}]

 :actor/non-goals
 [{:ng/id "N1" :ng/req "NOT a synthesis-recipe / weapons-design tool — no synthesis route, precursor, enrichment, or processing procedure is representable (G1)"}
  {:ng/id "N2" :ng/req "NOT a materials FAB or lab — datafication / mirror only; lab-in-the-loop is R3+ Council-gated (G2)"}
  {:ng/id "N3" :ng/req "does NOT run MLIP/ML models in R0/R1 — mirrors disclosed source values; model execution is R2+ on owned/donated compute only (G6)"}
  {:ng/id "N4" :ng/req "NOT the silicon iwakura/fuigo chip track nor the hotaru III-V substrate commons — open materials-discovery DATA mirror only (it feeds them)"}
  {:ng/id "N5" :ng/req "NOT a make/buy/trade recommender — non-adjudicating; surfaces a research shortlist, never a procurement or market decision (G3)"}
  {:ng/id "N6" :ng/req "no proprietary / vendor materials formulations — open-license sources only (G4)"}]

 :actor/cells
 [{:cell/id "materials_analyze"  :cell/kind :clojure :cell/method "methods/analyze.cljc" :cell/fn "analyze + render-report" :cell/runtime :bb}
  {:cell/id "coverage_report"    :cell/kind :clojure :cell/method "methods/analyze.cljc" :cell/fn "render-coverage" :cell/runtime :bb}
  {:cell/id "datom_emit"         :cell/kind :clojure :cell/method "methods/analyze.cljc" :cell/fn "render-datoms" :cell/runtime :bb}
  {:cell/id "materials_ingest"   :cell/kind :clojure :cell/method "methods/ingest.cljc" :cell/runtime :bb :cell/status :r1 :cell/gated "G7 — Council + operator DID; live Materials Project REST / OMat24 dump"}
  {:cell/id "materials_publish"  :cell/kind :clojure :cell/method "methods/publish.cljc" :cell/runtime :bb :cell/status :r1 :cell/gated "G7 — operator/mesh-side; ipfs pin + IPNS + 80-data/open-materials snapshot"}]

 :actor/lex
 [{:lex/id "materialRecord"}
  {:lex/id "datasetSourceAttestation"}
  {:lex/id "discoveryCandidateReport"}]

 :actor/substrate
 {:state    "kotoba Datom log (ADR-2605312345) — methods/analyze.cljc render-datoms emits canonical EAVT"
  :schema   "00-contracts/schemas/open-materials-ontology.kotoba.edn"
  :inference "Murakumo-only (ADR-2605215000); NO MLIP/ML model execution in R0/R1 (G6)"
  :runtime  "babashka (bb) — pure Clojure analyzer, no third-party deps; classpath root 20-actors"
  :tests    "bb --classpath 20-actors -e \"(require 'masago.methods.test-analyze)(clojure.test/run-tests 'masago.methods.test-analyze)\" — 13 tests green"}}
