;; kanjo — actor manifest (converted from manifest.jsonld, jsonld-retirement wave)
{:actor/glyph "勘定"
 :actor/display-name "勘定 (かんじょう)"
 :actor/cells [{"id" "cell:kanjo.concept_map", "method" "methods/concept_map.py", "input" "(none — the canonical concept catalogue is in-code)", "output" ["out/concept-dictionary.kotoba.edn"], "pipeline" "canonical concept catalogue → reverse index (jgaap/usgaap/ifrs element → canonical) → :fin.concept/* dictionary datoms. The G5 :synthesized GAAP-normalization layer; honest where standards are not comparable (経常利益 = JGAAP-only)."} {"id" "cell:kanjo.ingest", "method" "methods/ingest.py", "input" "data/ingest/*.json (SEC EDGAR companyfacts JSON or EDINET pre-extracted element JSON) or seed", "output" ["data/facts.merged.kotoba.edn"], "pipeline" "primary filing → element → canonical concept (concept_map) → :fin.filing/:fin.fact EAVT → merge with seed (:authoritative wins over :representative)", "gate" "G7 — live EDGAR/EDINET fetch requires KANJO_OPERATOR_GATE=1 + explicit --fetch-edgar CIK (single polite request, never an organism-tick scrape; passive-only per ADR-2605262400 §7). Offline/seed default, tagged :representative (G5)"} {"id" "cell:kanjo.analyze", "method" "methods/analyze.py", "input" "data/seed-financial-facts.kotoba.edn (or data/facts.merged.kotoba.edn)", "output" ["out/intel-report.md" "out/financial-metrics.kotoba.edn"], "pipeline" "facts → per-company per-FY ratios (:fin.metric :synthesized) → YoY (as-of history) → sector/currency aggregates (:fin.agg, coverage-honest, NO cross-currency FX) → aggregate-first report. Non-adjudicating, no advice."}]
 :actor/gates [#:gate{:id "G1", :rule "PRIMARY public disclosure ONLY — EDINET / SEC EDGAR / Companies House / EU OAM, all Tier-A (ADR-2605263800 §2). PROHIBITED inputs: 会社四季報 (paid copyrighted editorial compilation + 業績予想) and all paid commercial terminals (Bloomberg / S&P Capital IQ / Refinitiv / FactSet / Moody's / D&B / Pitchbook / Crunchbase) per Charter Rider §2(e) anti-gatekeeping + §2(c). Read the filing, never the terminal."} #:gate{:id "G2", :rule "NON-ADJUDICATING — records disclosed facts + transparent ratios. Does NOT rate good/bad, value a company, rule fraud/solvency, or label a company. Sibling of kabuto G4 / danjo. A transparency map, never a verdict."} #:gate{:id "G3", :rule "aggregate-first — sector / coverage aggregates surface first; per-company facts mirror the filing verbatim."} #:gate{:id "G4", :rule "NO investment advice / NO securities recommendation (金商法 — NOT 投資助言業) — no buy/sell/hold, no price targets, no ratings, no portfolio. No forecasting (no 業績予想 — exactly what 四季報 adds)."} #:gate{:id "G5", :rule "sourcing honesty (:authoritative parsed-from-XBRL | :representative seed/headline | :synthesized derived). Metrics + aggregates are :synthesized, never re-ingested as disclosed facts. Σ aggregates are coverage-bounded, never a market total."} #:gate{:id "G6", :rule "Murakumo-only narration (ADR-2605215000)."} #:gate{:id "G7", :rule "outward-gated INGEST — live EDGAR/EDINET fetch = KANJO_OPERATOR_GATE + Council; full-universe XBRL parse is R1. Passive-only (ADR-2605262400 §7), no organism-tick scraping."} #:gate{:id "G8", :rule "no git-lfs — source XBRL/PDF filings → DataLad → IPFS (80-data/corporate-financials); CID on :fin.filing/doc-cid."} #:gate{:id "G9", :rule "PII / redaction — filings name officers/directors. kanjō ingests the FINANCIAL line items, not the person roster; any incidental personal data follows the ADR-2605263800 §5 per-jurisdiction redaction policy (GDPR / 個人情報保護法) → encrypted envelope, excluded by default."} #:gate{:id "G10", :rule "NO market-abuse enablement — published filings only; never non-public material facts or pre-disclosure insider data (金商法)."} #:gate{:id "G11", :rule "restatement-as-history (非終末論) — a 訂正報告書 asserts a NEW fact via :fin.fact/superseded-by; the prior Datom is RETAINED, never deleted. Read as-of."} #:gate{:id "G12", :rule "read-only — never mutates upstream filings; kanjō is an observer."}]
 :actor/adr "2606032000"
 :actor/tier :tier-b
 :actor/integrates ["danjo" "kabuto" "kanae" "toritate" "tsumugi"]
 :actor/purpose "Datafies the world's PUBLIC (listed) companies' DISCLOSED financial facts — the balance sheet (貸借対照表), income statement (損益計算書) and cash-flow statement (キャッシュフロー計算書) line items from PRIMARY disclosure (JP EDINET 有価証券報告書 + US SEC EDGAR 10-K/20-F + UK Companies House + EU OAM, all Tier-A per ADR-2605263800) — into the kotoba Datom log as `:fin.fact/*`, normalized across JP-GAAP / US-GAAP / IFRS onto canonical concepts. Surfaces aggregate-first sector / health observations routed to transparency + financial resilience. The EXTERNAL public-company sibling of toritate 執帳 (internal accounting) and the financials face of kabuto 兜 (supply chain). NON-ADJUDICATING and gives NO investment advice — it records what the company disclosed, never a verdict, rating, valuation, or recommendation."
 :actor/non-goals ["N1 — NOT investment advice / 投資判断 (no buy/sell/hold, no targets, no portfolio)." "N2 — NOT a credit rating agency (no ratings / scores / outlooks)." "N3 — NOT 四季報 / no commercial-terminal data (Tier-C prohibited)." "N4 — NOT fraud / solvency adjudication (records facts + flags arithmetic; ruling is a state/court matter — danjo-style observation boundary)." "N5 — NOT forecasting / 業績予想 (reported actuals only)." "N6 — NOT real-time market data / quotes / valuation." "N7 — NOT private-company financials (public-disclosure filers only)." "N8 — NOT tax / accounting advice (税務・会計助言なし)."]
 :actor/lexicons ["com.etzhayyim.kanjo.registerFiling" "com.etzhayyim.kanjo.registerFinancialFact" "com.etzhayyim.kanjo.publishConceptDictionary" "com.etzhayyim.kanjo.publishIntelReport"]
 :actor/status :r0-design-only
 :actor/related ["2605263800" "2606022000" "2604291500" "2605262900" "2605301600" "2605262130" "2605312345" "2605215000" "2606013600" "2606013800"]
 :actor/id "kanjo"
 :substrate {:datasets [{:dataset-id "kanjo-coverage"
                         :layer :repo
                         :source "SEC EDGAR / EDINET (Tier-A primary disclosure, ADR-2605263800)"
                         :cidv1 "bafybeiae7xbotq4m2m55mycpsh3qrn4g67xz52dporyf4sfxoj6hcj7quq"
                         :freshness-days 0
                         :retrieved "2026-06-16"
                         :counts-toward-world-coverage false
                         :provenance "80-data/coverage-manifests/kanjo-coverage-manifest.json"}]}}
