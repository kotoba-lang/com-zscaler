;; meisai — actor manifest (converted from manifest.jsonld, jsonld-retirement wave)
{:actor/glyph "明細"
 :actor/display-name "明細 — Member Card-Statement Ingestion"
 :actor/adr "2606122400"
 :actor/tier :tier-b
 :actor/integrates ["kaiyaku" "karakuri" "organizer" "toritate" "warifu"]
 :actor/purpose "Member-own card-statement (利用明細) ingestion into the local kotoba Datom log. The member runs a READ-ONLY computer-use fetch leg on their OWN machine against their OWN card account (com-junkawasaki/computer-use-clj sumitclub_meisai — karakuri T2 posture, vault-injected credentials, Murakumo-conformant local inference per ADR-2605215000); meisai ingests the resulting local statement EDN into append-only :meisai.stmt/* + :meisai.row/* EAVT datoms. NO network I/O, NO credential held, credential/PAN shapes UNREPRESENTABLE (raise at ingest), personal financial data stays under the gitignored data/ (never committed, pinned, or published)."
 :actor/non-goals ["N1 — not a household-accounting app or budgeting advisor (no advice, no scoring of spending)" "N2 — not a scraper service for third parties (member-own, member-run only)" "N3 — not a payment instrument (warifu is the corp's card rail; meisai only reads statements)" "N4 — not a publication pipeline (statements are private; aggregate publication would be a future Council-gated ADR)" "N5 — not a credential store (vault stays at the member's host)"]
 :actor/lexicons ["com.etzhayyim.meisai.statement (cells/lex/meisai.statement.json — statement header; PERSONAL, local-only, no PAN/credential)" "com.etzhayyim.meisai.source (cells/lex/meisai.source.json — coverage source; PUBLIC metadata)" "com.etzhayyim.meisai.recurringHandoff (cells/lex/meisai.recurringHandoff.json — the meisai→kaiyaku recurring-charge handoff; PERSONAL, local-only)"]
 :actor/status :r1-worldwide-coverage-registry-101-sources-multi-currency-normalize-recurring-charge-kaiyaku-handoff-round-trip-closed-report-time-fx-3-lexicons-member-local-launchd-heartbeat-28-bb-tests-green-per-issuer-fetch-adapters-member-side-are-the-remaining-r1-work-first-supported-source-sumitclub-jp-sumi-trust-club-
 :actor/id "meisai"}
