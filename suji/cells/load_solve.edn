[{:cell/entry "cells/load_solve/cell.py", :cell/node "gad", :cell/id "load_solve", :cell/glyph "筋", :cell/kind :langgraph, :cell/runtime :wasm, :db/id -1, :cell/gates ["G1" "G9" "G10"], :cell/state-graph "{:nodes [:read-posture :static-inverse-dynamics :muscle-distribute :assert-nondiagnostic :emit], :entry :read-posture, :edges [[:read-posture :static-inverse-dynamics] [:static-inverse-dynamics :muscle-distribute] [:muscle-distribute :assert-nondiagnostic] [:assert-nondiagnostic :emit] [:emit :end]]}", :cell/doc "Solve the static inverse dynamics for a posture (methods/load.py + muscle.py) — the coded heart. Posture joint angles → per-joint gravitational moment (RNEA gravity term, the kami-genesis PlanarChain statics) → cervical compressive load (Hansraj-validated) → Hill-type muscle %MVC (緊張). Enforces NON-DIAGNOSTIC by construction (G1): outputs are forces, never a diagnosis. Emits jointLoad + muscleTension Datoms. R0 scaffold (.solve() raises; transitions are unit-tested).", :cell/handler "LoadSolveCell", :cell/kotoba "{:reads [:posture/head-flex-deg :posture/trunk-flex-deg :posture/shoulder-flex-deg :posture/arms-supported], :writes [:load/id :load/moment-nm :load/compressive-kgf :muscle/id :muscle/mvc-pct]}"}]