[{:cell/entry "cells/ergonomic_compare/cell.py", :cell/node "naphtali", :cell/id "ergonomic_compare", :cell/glyph "筋", :cell/kind :langgraph, :cell/runtime :wasm, :db/id -1, :cell/gates ["G1" "G3" "G5" "G9"], :cell/state-graph "{:nodes [:gather-scenarios :diff :narrate :emit], :entry :gather-scenarios, :edges [[:gather-scenarios :diff] [:diff :narrate] [:narrate :emit] [:emit :end]]}", :cell/doc "Compare the same body across workstation setups (methods/analyze.py): baseline (laptop-on-lap) vs improved (external monitor at eye level) → cervical-load reduction + worst-muscle shift + the physical mechanism. SELF-REFERENCED Wellbecoming (G3), NOT medical advice and NOT a person ranking; any LLM narration via Murakumo only (G5). Emits an ergonomicComparison Datom. R0 scaffold (.solve() raises).", :cell/handler "ErgonomicCompareCell", :cell/kotoba "{:reads [:load/compressive-kgf :strain/stiffness], :writes [:strain/id]}"}]