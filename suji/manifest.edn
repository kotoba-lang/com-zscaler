{:actor/id        "suji"
 :actor/glyph     "筋"
 :actor/tier      :tier-b
 :actor/integrates ["iyashi" "kizashi" "kokoro" "mitate"]
 :actor/domain    "suji.etzhayyim.com"
 :actor/did       "did:web:etzhayyim.com:actor:suji"
 :actor/purpose   "Musculoskeletal posture-load biomechanics simulator — the PHYSICS-SIMULATION sibling of kizashi 兆 (which SENSES the body) and upstream of mitate (which DIAGNOSES). 筋 = muscle/sinew AND line-of-force/reasoning, the double meaning of a force-balance engine. Answers 'what does a laptop posture do to the body?' with runnable, validated physics: a sagittal articulated segment chain (head→cervical→thorax→lumbar + arm) built from de Leva/Winter anthropometry — the same PlanarChain kami-genesis solves — driven through STATIC inverse dynamics (Featherstone RNEA gravity term) → per-joint moment + cervical compressive load (VALIDATED against Hansraj 2014's forward-head-posture table) → Hill-type muscle %MVC (緊張/tension) → Rohmert sustained-isometric dose (強張り/stiffness) over a work session. Compares laptop-on-lap vs laptop-on-desk vs external-monitor-at-eye-level. NON-DIAGNOSTIC by construction (G1, 医師法 §17): every output is a mechanical quantity (moment/force/%MVC/kgf), and no diagnosis/disease/prescription/treatment field is representable anywhere (schema + lexicon + cell-gate, the nusa/tazuna/kamado pattern). A licensed clinician (mitate/iyashi) owns any health interpretation. Wires to kami-genesis / nv-compat Isaac-Sim via a WIT articulation contract (gated backend; the submodule is unpopulated — the noroshi pattern)."
 :actor/status    :r0
 :actor/adr       "2606061900"
 :actor/related   ["2605312700" "2605260100" "2605263000" "2605261000" "2605311500" "2605311800" "2606010030" "2606010600" "2605261800" "2606051600" "2605231525" "2605215000" "2605241900" "2605312345" "2605181100" "2606042330" "2605192100"]
 :actor/siblings  ["kizashi" "mitate" "iyashi" "kokoro" "kami-genesis"]
 :actor/labor
 {:isic   ["Q86" "M72"]
  :isco   ["2269" "2149"]
  :unspsc ["42" "85"]
  :lps-rank nil
  :note "biomechanics simulation + ergonomic analysis; an instrument/sim layer, not a clinical service (mitate/iyashi own clinical adjudication)"}

 :actor/charter-rider {:rider/version "2.0" :rider/applied true}

 ;; L4 Care Tier — instrument/simulation layer, UPSTREAM of clinical adjudication.
 :actor/tier-role :biomechanics-simulation
 :actor/pipeline ["kizashi senses → suji simulates the loads → mitate diagnoses → iyashi treats"]

 :actor/gates
 [{:gate/id "G1" :gate/name "non-diagnostic" :gate/req "医師法 §17 — every output is a mechanical quantity (moment N·m, force N, %MVC, kg-force, stiffness dose). NO diagnosis / disease / icd / prescription / treatment / condition field is representable in schema, lexicon, OR the load_solve cell (assert_nondiagnostic refuses a clinical key by construction; nusa/tazuna/kamado precedent). A licensed clinician (mitate/iyashi) owns any diagnosis"}
  {:gate/id "G2" :gate/name "simulation-only / not-a-medical-device" :gate/req "薬機法/SaMD — R0..R2 are pure physics simulation over posture PARAMETERS; NO sensing hardware and NO biometric capture (that is kizashi 兆). suji never touches a patient"}
  {:gate/id "G3" :gate/name "self-referenced-wellbecoming" :gate/req "a stiffness trajectory is as-of and compared only against the SAME member's other postures (the choice 'raise the screen / support the arms'); NEVER a population ranking / score-of-soul (kizashi G8; 非終末論, no final state)"}
  {:gate/id "G4" :gate/name "encrypted-envelope-on-real-scan" :gate/req "ADR-2605181100 — a body model BUILT FROM a real kizashi scan carries 要配慮 PII and MUST set encryptedPayloadCid; synthetic/parametric bodies are public. suji's own reference bodies are :representative population averages"}
  {:gate/id "G5" :gate/name "murakumo-only" :gate/req "any LLM-assisted ergonomic narration via LiteLLM 127.0.0.1:4000 / edge Murakumo only (ADR-2605215000); vendor medical-AI PROHIBITED"}
  {:gate/id "G6" :gate/name "no-server-key" :gate/req "a real-scan-fed solve is member/operator-signed; the platform NEVER holds the signing key (ADR-2605231525); serverHeldKey=false"}
  {:gate/id "G7" :gate/name "sourcing-honesty" :gate/req ":representative anthropometry (de Leva/Winter), Rohmert endurance, Hill muscle parameters; the cervical leg is VALIDATED vs Hansraj 2014, the muscle/strain legs are mechanistically grounded but illustrative; NO silicon/hardware, NO live member scan"}
  {:gate/id "G8" :gate/name "outward-gated" :gate/req "a live kizashi posture feed / a live kami-genesis forward-dynamic sim / any clinical handoff to mitate is Council Lv6+ + operator gated; R0-R2 = parametric simulation only"}
  {:gate/id "G9" :gate/name "kotoba-eavt-audit" :gate/req "every body / posture / joint-load / muscle-tension / strain = Datoms (as-of, replayable; 非終末論); canonical state in the kotoba Datom log, never RisingWave (ADR-2605312345)"}
  {:gate/id "G10" :gate/name "anti-pseudoscience" :gate/req "mechanically-grounded biomechanics ONLY (moment balance, Hill muscle, Rohmert endurance); 経絡 / 気 / 波動 / aura stiffness theories are EXCLUDED (kizashi N8); muscle groups come from the Hill-model SPECS table"}]

 :actor/non-goals
 ["N1 not a diagnosis or treatment device — outputs forces, never a disease (G1, 医師法 §17)"
  "N2 not a medical device — no sensing hardware, no biometric capture (kizashi owns sensing; SaMD/薬機法 boundary)"
  "N3 not a fitness/person ranking — self-referenced trajectory only (G3)"
  "N4 not an ergonomics regulatory-compliance attestation (ISO 9241 / JIS) — a physics model, not a certified assessment"
  "N5 Murakumo-only inference; no commercial GPU / cloud medical-AI"
  "N6 no live actuation — the body model is passive, never a command to a powered exosuit/robot (tazuna force-class boundary)"]

 :actor/cells
 [{:cell/id "segment_build"      :cell/node "naphtali" :cell/coded false}
  {:cell/id "posture_resolve"    :cell/node "gad"      :cell/coded false}
  {:cell/id "load_solve"         :cell/node "gad"      :cell/coded true}
  {:cell/id "strain_accumulate"  :cell/node "asher"    :cell/coded true}
  {:cell/id "ergonomic_compare"  :cell/node "naphtali" :cell/coded false}]

 :actor/lexicons
 ["com.etzhayyim.suji.bodyModel"
  "com.etzhayyim.suji.postureScenario"
  "com.etzhayyim.suji.jointLoad"
  "com.etzhayyim.suji.muscleTension"
  "com.etzhayyim.suji.strainReport"
  "com.etzhayyim.suji.ergonomicComparison"]

 :actor/methods
 {:segment   "methods/segment.py  — anthropometric sagittal segment chain (de Leva/Winter)"
  :posture   "methods/posture.py  — laptop workstation → sagittal joint angles"
  :load      "methods/load.py     — static inverse dynamics; cervical load validated vs Hansraj 2014"
  :muscle    "methods/muscle.py   — Hill-type moment-arm muscle %MVC (緊張)"
  :strain    "methods/strain.py   — Rohmert sustained-isometric dose → stiffness index (強張り)"
  :analyze   "methods/analyze.py  — end-to-end laptop posture comparison report"
  :bridge    "methods/kami_biomech_bridge.py — kami-genesis/Isaac articulation contract (wit/kami-biomech.wit)"}

 :actor/honest-r0
 "Design + runnable physics + validated cervical model only. Anthropometry/muscle/endurance parameters are :representative population values (G7); the cervical leg reproduces Hansraj 2014 within ~10%, the muscle %MVC and Rohmert strain legs are mechanistically grounded but illustrative. No hardware, no live member scan, no live kami-genesis backend (the 40-engine/kami-engine submodule is unpopulated → the Isaac/kami wiring is a WIT contract + Python reference, the noroshi pattern). Cells .solve() raise at R0; the load_solve transitions are unit-tested. Live kizashi feed / clinical handoff Council Lv6+ + operator gated."}
