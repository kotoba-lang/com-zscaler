;; shionome — actor manifest (converted from manifest.jsonld; :actor/manifest holds the verbatim jsonld content)
{:actor/id "shionome"
 :actor/glyph "潮目"
 :actor/display-name "潮目 — Cross-Asset Capital-Flow Observatory"
 :actor/tier :tier-b
 :actor/purpose "Ingests PUBLIC market data across asset classes (equities / govt-bonds / credit / commodities / FX / crypto / real-estate / cash) and weaves OBSERVED capital rotation — どこからどこへ資金が流れているか — into one kotoba Datom flow-graph + dry-run social posts. Observational mirror of where money MOVED; NEVER trades, never a buy/sell signal, never a price target (トレードはしない). Aggregate-first, edge-primary, non-advisory."
 :actor/adr ["2606072200" "2606051800"]
 :actor/manifest
 {"primarySchema" "00-contracts/schemas/capital-flow-ontology.kotoba.edn", "gates" {"G7" "no-server-key — social posts member-signed; serverHeldKey const false; server never signs (ADR-2605231525)", "G11" "sourcing-honesty — :representative vs :authoritative declared on every datom; the committed seed is :representative; registry verification status drives :authoritative", "G9" "PII-encrypted — public-bucket only; any incidentally-sensitive datum → XChaCha20-Poly1305 envelope (ADR-2605181100)", "G10" "non-eschatological as-of — flows/snapshots are append-only observation history on the content-addressed Datom DAG; a re-observation is a NEW datom, never an overwrite", "G5" "mirror-not-signal — every output is an observational map with the disclaimer; never a trade signal, never financial advice, never speaks AS a market maker", "G6" "Murakumo-only inference/narration (ADR-2605215000)", "G8" "outward-gated — live market-data ingest + live posting = Council Lv6+ + operator + member signature; R0 = offline analyzer + dry-run posts + autonomous loop that persists to the LOCAL kotoba log only (no external I/O); cells .solve() raise", "G1" "public-bucket-only / no-doxxing — a node is a public capital bucket (asset-class / sector / region / theme); a person / account / portfolio / individual investor is unrepresentable (schema :db/allowed + lexicon + ValueError)", "G3" "source-provenance mandatory — ≥2 public-source citations per flow; commercial market-data terminals (Bloomberg/Refinitiv/FactSet/CapIQ/四季報…) are a prohibited citation (Rider §2(e)/N5)", "G4" "edge-primary, no rating-of-an-asset — concentration computed on read from flows; no per-bucket rating/signal/target/score (those would be trade instructions)", "G2" "no-trade / non-advisory (トレードはしない) — flow + snapshot kinds are FACTUAL observations; trade/advisory tokens (buy / sell / long / short / recommend / overweight / target-price / 推奨 / 買い / 売り) are unrepresentable on flows, buckets, AND post bodies; noTradeNotice const true"}, "displayNameJa" "潮目", "@context" ["https://www.w3.org/ns/activitystreams" "https://etzhayyim.com/ns/actor-manifest"], "integrates" ["actor:mitooshi" "actor:kanae" "actor:kanjo" "actor:watari" "actor:kabuto" "actor:tsumugi" "actor:keizu"], "displayName" "潮目 — Cross-Asset Capital-Flow Observatory", "id" "did:web:etzhayyim.com:actor:shionome", "substrate" {"state" "kotoba Datom log (ADR-2605312345) — append-only content-addressed EAVT (data/shionome.datoms.kotoba.edn at R0)", "inference" "Murakumo-only (ADR-2605215000)", "largeAssets" "DataLad → IPFS — NO git-lfs"}, "crossActorBoundary" ["mitooshi (ADR-2606051800) — probabilistic FORECASTS (distribution-only, never trades); shionome observes REALIZED flows and never forecasts (disjoint object, shared never-trade discipline). shionome flows can become a mitooshi observation input." "kanae (ADR-2605302300) — renders fund flows (Sankey/treemap); shionome emits the capital-movement :flow datoms kanae visualizes." "kanjo (ADR-2606032000) — company financial DISCLOSURE KG (BS/PL/CF); shionome is cross-asset price/flow movement, not company fundamentals (disjoint object)." "watari (ADR-2606041827) — live physical positions (ship/aircraft); shionome is capital positions; both append-only as-of, no person-tracking." "kabuto (ADR-2606022000) — supply-chain concentration; shionome is capital-flow concentration (shared HHI/edge-primary discipline, disjoint domain)." "tsumugi/keizu — power-entity / government relations; shionome is markets (disjoint), shares the mirror-not-target + edge-primary discipline."], "name" "shionome", "roadmap" [{"phase" "R0", "date" "2026-06-07", "scope" "ontology + 4 lexicons + :representative seed + analyzer (weave/concentration/social/ingest/export) + kotoba Datom-log writer + AUTONOMOUS heartbeat loop (offline, dry-run, content-addressed DAG) + 5 cell scaffolds (.solve() raise) + tests. Design + offline + dry-run + autonomous-over-local-log only.", "gate" "ADR-2606072200 (PROPOSED)"} {"phase" "R1", "date" "post-Council Lv6+ ≥1 ratify", "scope" "ingest + flow_graph + rotation_weave build kotoba EAVT datoms over offline public-source batches; no live posting.", "gate" "Council Lv6+ ≥3 per cell"} {"phase" "R2", "date" "post-R1 + 30-day public objection", "scope" "+regime_observer on live read-path; first dry-run networkPosts reviewed.", "gate" "Council Lv6+ ≥4 + 30-day public comment"} {"phase" "R3", "date" "post-R2 + Council Lv7+", "scope" "+social_post live publication under 1 SBT = 1 vote + member signature; live public-market-data ingest.", "gate" "Council Lv7+ + operator"}], "adr" {"master" "etzhayyim:adr:2606072200", "parent" "etzhayyim:adr:2606051800", "r0" "etzhayyim:adr:2606072200", "r1" "reserved", "r2" "reserved", "r3" "reserved"}, "status" "R0-design-plus-autonomous-offline", "lexiconNamespaces" ["com.etzhayyim.shionome.capitalFlowObservation" "com.etzhayyim.shionome.bucketSnapshot" "com.etzhayyim.shionome.rotationFinding" "com.etzhayyim.shionome.networkPost"], "displayNameEn" "Shionome", "cells" [{"name" "ingest", "module" "magatama.cells.shionome_ingest", "murakumoNode" "issachar", "phase" "continuous", "io" "public market-data records (exchange / fund-flow / central-bank / on-chain / index) → validated :bucket/:flow/:snap datoms (no-trade screened)"} {"name" "flow_graph", "module" "magatama.cells.shionome_flow_graph", "murakumoNode" "issachar", "phase" "continuous", "io" "validated flows → per-bucket inflow/outflow net-flow index (capital-movement kinds only)"} {"name" "rotation_weave", "module" "magatama.cells.shionome_rotation_weave", "murakumoNode" "dan", "phase" "continuous", "io" "flow edges → aggregate edge-primary metrics (net flow / rotation pairs / inflow HHI / by-asset-class / by-region)"} {"name" "regime_observer", "module" "magatama.cells.shionome_regime_observer", "murakumoNode" "dan", "phase" "periodic", "io" "net flow + risk tags → FACTUAL cross-asset regime descriptor (risk-on/off/mixed); descriptive, never advice"} {"name" "social_post", "module" "magatama.cells.shionome_social_post", "murakumoNode" "naphtali", "phase" "periodic (event)", "io" "aggregate finding → dry-run networkPost (member-signed, mirror disclaimer, no-trade body scan, ≥2 sources); live post Council Lv6+ + operator gated"}], "descriptionJa" "潮目 (shionome) — クロスアセットの資金フロー観測所。公開市場データ(取引所データ・資金フローレポート・中央銀行公表・オンチェーン暗号資産・指数プロバイダ)を ingest し、世界の資金を公的な器(資産クラス/セクター/地域/テーマ)のグラフとしてモデル化、観測された資金移動 — 器ごとの純流入(どこに資金が向かっているか)・回転ペア(どこからどこへ)・流入集中度(HHI)・クロスアセットのレジーム記述(リスクオン/オフ/まちまち) — を一つの kotoba Datom フローグラフに編み、集計所見を dry-run social post として narrate する。最重要不変条件は トレードはしない(G2): 売買シグナル・目標価格・オーバー/アンダーウェイト・ポジション提案を一切出さない — trade/advisory トークンは flow・bucket・post body 上で構造的に表現不能で、全出力に noTradeNotice=true。観測ミラー(G5)であってシグナルではない。集計・edge-primary(G4)で個別の器に格付け/スコアを持たない。出典必須(G3, フローごとに ≥2 公開出典)。no-doxxing(G1/G9 — 器は公的な資産カテゴリで、個人・口座・ポートフォリオではない)。no-server-key(G7)+ outward-gated(G8): live market-data ingest と live posting は Council Lv6+ + operator + 本人署名 gated。R0 は設計 + offline analyzer + dry-run posts + append-only kotoba Datom log に content-addressed トランザクションを永続化する自律ハートビートループ(live 外部 I/O は gated のまま)。", "tier" "Tier-B", "primaryLexicon" "com.etzhayyim.shionome.capitalFlowObservation", "glyph" "潮目", "@type" "ActorManifest", "nonGoals" {"N1" "NOT a trading system / signal service / robo-advisor (トレードはしない — no broker, no order book, no execution, no buy/sell signal, no price target)", "N2" "NOT investment advice or a recommendation engine (non-advisory; informational mirror only)", "N3" "NOT a per-asset rating / score / ranking-to-buy", "N4" "NOT a surveillance system of individual investors / accounts / portfolios (public market aggregates only; no-doxxing)", "N5" "NOT a commercial market-data terminal (Bloomberg / Refinitiv / FactSet / CapIQ / 四季報 prohibited as source)", "N6" "NOT a forecaster (mitooshi owns probabilistic forecasts; shionome observes realized flows)", "N7" "NOT a RisingWave / SQL / Datomic store — kotoba Datom log only"}, "description" "潮目 (shionome) — a cross-asset capital-flow observatory. It ingests PUBLIC market data (exchange data, fund-flow reports, central-bank releases, on-chain crypto, index providers), models the world's capital as a graph of public BUCKETS (asset-class / sector / region / theme), and weaves OBSERVED capital movement — net flow per bucket (where money is going/leaving), rotation pairs (どこからどこへ), inflow concentration (HHI), and a FACTUAL cross-asset regime descriptor (risk-on/off/mixed) — then narrates aggregate findings as DRY-RUN social posts. THE DEFINING INVARIANT is トレードはしない (G2 no-trade): shionome NEVER emits a buy/sell signal, a price target, an over/under-weight call, or a portfolio instruction — trade/advisory tokens are structurally unrepresentable on flows, buckets, and post bodies, and every output carries noTradeNotice=true. An observational MIRROR (G5), never a trade signal; aggregate + edge-primary (G4) with no per-bucket rating/score; sourced (G3, ≥2 public-source citations per flow); no-doxxing (G1/G9 — buckets are public asset categories, never persons/accounts/portfolios). no-server-key (G7) + outward-gated (G8): live market-data ingest + live posting are Council Lv6+ + operator + member-signature gated; R0 = design + offline analyzer + dry-run posts + an AUTONOMOUS heartbeat loop that persists content-addressed Datom transactions to the append-only kotoba Datom log (live external I/O stays gated).", "purpose" "Ingests PUBLIC market data across asset classes (equities / govt-bonds / credit / commodities / FX / crypto / real-estate / cash) and weaves OBSERVED capital rotation — どこからどこへ資金が流れているか — into one kotoba Datom flow-graph + dry-run social posts. Observational mirror of where money MOVED; NEVER trades, never a buy/sell signal, never a price target (トレードはしない). Aggregate-first, edge-primary, non-advisory."}}
