# tsubasa 翼 — the append-only fare-observation ledger is generated by the
# autorun heartbeat (content-addressed, resume-safe). It is reproducible from the
# committed seed, so it is NOT tracked. The seed (data/seed-fares.kotoba.edn) IS.
data/persisted/
