;; 20-actors/kizuna/schema.edn — Datomic/Datascript-compatible schema for kizuna's
;; datomized .edn files (auto-generated by manifest/edn-datomize.cljs semantics,
;; hand-run for this actor per the Phase 4 fan-out). :db/ident attr defs list.
;; No Datomic-specific keys (:db.install/_attribute etc). Do not hand-edit —
;; regenerate if seed-interactions.kotoba.edn's shape changes.

[#:db{:ident :seed-interactions/events, :valueType :db.type/string, :cardinality :db.cardinality/one}]
