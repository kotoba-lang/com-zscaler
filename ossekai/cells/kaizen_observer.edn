[{:cell/entry "py/agent.py", :cell/id "kaizen_observer", :cell/glyph "御節介", :cell/kind :datalog, :cell/runtime :kotoba, :db/id -1, :cell/gates ["G4" "G5" "G6" "G10" "G14"], :cell/doc "Quarterly self-reflection audit (KaizenObserverCell pattern). Computes the silenOssekaiReview record and DECIDES self-corrections from the constitutional invariants: aggregate-share ≥50% (G4/G14) else next-quarter mention cap is structurally halved; reEngagementAfterOptOut MUST be 0 (G14) else CRITICAL halt + chigiri.disputeMediation; commercial-CRM penetration MUST be 0 (G5) else CRITICAL halt; framing-drift / unsubscribe-rate spikes emit KaizenProposals (rules R12..R17). This is the actor's deploy-autonomy governance layer — it self-throttles, no human in the loop for the routine case.", :cell/handler "handle_kaizen_observer", :cell/kotoba "{:reads [:metric/aggregate-posts :metric/targeted-dispatches :metric/re-engagement-after-opt-out :metric/unsubscribe-count :metric/commercial-crm-pct :metric/framing-audit-failures :metric/deliveries], :writes [:review/aggregate-share-pct :review/halt :review/throttle-factor :review/re-engagement-after-opt-out :review/commercial-crm-pct]}"}]
