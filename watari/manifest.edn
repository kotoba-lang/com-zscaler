;; watari — actor manifest (converted from manifest.jsonld, jsonld-retirement wave)
{:actor/glyph "渡り"
 :actor/display-name "渡り (わたり)"
 :actor/cells [{"id" "cell:watari.ingest", "method" "methods/ingest.py", "status" "R0 stub — design only", "input" "data/ingest/*.json (AISStream / OpenSky-shaped public broadcast batch)", "output" ["data/craft-graph.merged.kotoba.edn"], "pipeline" "public AIS/ADS-B batch → normalize to :craft/:craft.fix → dedup craft identity (seed wins) → append fixes", "gate" "G7 — live AISStream/OpenSky fetch requires WATARI_OPERATOR_GATE (Council); offline/sample mode default, tagged :representative (G5)"} {"id" "cell:watari.analyze", "method" "methods/analyze.py", "input" "data/seed-craft-graph.kotoba.edn (or data/craft-graph.merged.kotoba.edn)", "output" ["out/intel-report.md" "out/movement-situation.kotoba.edn"], "pipeline" "classify → latest as-of fix per craft → lane/corridor load (by kind) → chokepoint transit (composes w/ watatsuna) → approach congestion → freshness tail (aggregate-first)"}]
 :actor/gates [#:gate{:id "G1", :rule "public transponder broadcasts only — AIS (ships) + ADS-B (aircraft) are open public signals; ingest only these + public registries (ITU MMSI, ICAO 24-bit, national reg). Non-broadcasting / military / blocked-from-display (FAA LADD, PIA) craft = OUT OF SCOPE"} #:gate{:id "G2", :rule "situational-awareness, not surveillance / not targeting — aggregate-first lane/density/congestion routed to safety + collision-avoidance + congestion-easing + resilience; NEVER a 'follow this craft' tool, NEVER a targeting feed (Charter Rider §2(a) force-separation + §2(d); mirrors watatsuna G2)"} #:gate{:id "G3", :rule "aggregate-first + claimed-first output"} #:gate{:id "G4", :rule "no person-tracking / no pattern-of-life — a craft is a craft, not a person; MUST NOT de-anonymize a private-craft owner/crew/passenger, build pattern-of-life on an individual, or answer 'where is person X'. Private-owner identity → encrypted/excluded"} #:gate{:id "G5", :rule "sourcing honesty (:authoritative | :representative | :synthesized) on every node/fix; no fabricated live coverage — absence = 'not yet ingested', a stale fix is reported as stale (freshness tail)"} #:gate{:id "G6", :rule "Murakumo-only narration (ADR-2605215000)"} #:gate{:id "G7", :rule "outward-gated — live AISStream/OpenSky/adsb.fi ingest = Council + operator; R0 ships a bounded seed only"} #:gate{:id "G8", :rule "no git-lfs; bulk position history / replay tiles → DataLad → IPFS (80-data/moving-craft)"} #:gate{:id "G9", :rule "no PII — craft graph only; owner/crew/passenger data → encrypted envelope, excluded by default"}]
 :actor/adr "2606041827"
 :actor/tier :tier-b
 :actor/integrates ["watatsumi" "watatsuna"]
 :actor/purpose "World live moving-craft knowledge graph — ingests the LIVE positions of public, transponder-broadcasting craft (ships via AIS, aircraft via ADS-B) into the kotoba Datom log as an append-only as-of trajectory; surfaces aggregate sea-lane / air-corridor / chokepoint / approach concentration routed to safety + collision-avoidance + congestion-easing + resilience. A situational-awareness map, NEVER a person-surveillance feed and NEVER a target-list. The kotoba-native successor to the legacy maps aismarine/aircraft_live + vessel tracking pipelines."
 :actor/non-goals {"N1" "not a weapons / targeting / fire-control feed (Charter §2(a) force-separation)", "N2" "not a person-surveillance / pattern-of-life tool (G4)", "N3" "not real-time control — observes craft, never flies/sails one (that is kami-autodrive / funadaiku / watatsumi)", "N4" "not a safety-of-life service — NOT GMDSS / ATC / VTS certified; situational-awareness only", "N5" "no RisingWave / SQL store — supersedes the legacy maps/vessel position stores", "N6" "no military / blocked-from-display craft de-anonymization"}
 :actor/lexicons ["com.etzhayyim.watari.registerCraft" "com.etzhayyim.watari.recordFix" "com.etzhayyim.watari.recordLeg" "com.etzhayyim.watari.registerLane"]
 :actor/status :r0-design-only
 :actor/related ["2606012600" "2605262130" "2605312345" "2605215000" "2605241500" "2605192200"]
 :actor/id "watari"}
