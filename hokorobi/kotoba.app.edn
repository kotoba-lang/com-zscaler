;; kotoba.app.edn — actor:hokorobi 綻び KOTOBA Mesh application manifest (generated scaffold).
;; Deploy:  kotoba app deploy kotoba.app.edn  (compiles methods/mesh.clj → CID → control datoms)
{:kotoba.app/name    "hokorobi"
 :kotoba.app/version "0.1.0"
 :kotoba.app/components
 [{:name     "hokorobi"
   :src      "methods/mesh.clj"                  ; Clojure default → run + on-kse
   :scale    1
   :triggers [{:type :kse :topic "etzhayyim/actor/hokorobi"}]  ; observatory on-kse (ADR-2606230001 §4)
   :requires #{:cap/kqe}}]

 :kotoba.app/placement {:spread  :zone
                        :require {:tier "edge"}}}
