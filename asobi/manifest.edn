;; asobi — actor manifest (converted from manifest.jsonld, jsonld-retirement wave)
{:actor/glyph "遊び"
 :actor/cells [{"id" "cell:asobi.analyze", "method" "methods/analyze.py", "input" "data/seed-asobi-graph.kotoba.edn", "output" ["out/participation-report.md"], "pipeline" "load-edn → edge-primary integral (Σ incident opening-load × disclosed access weight) → participation-openness + enclosure-load + enclosure-concentration, aggregate-first", "doc" "edge-primary participation/enclosure; access map, never an engagement ranking"} {"id" "cell:asobi.datom-emit", "method" "methods/datom_emit.py", "input" "data/seed-asobi-graph.kotoba.edn", "output" ["out/asobi-datoms.kotoba.edn"], "pipeline" "ground EAVT datoms [e a v tx :add] (durable) + derived readouts flagged :bond/is-transient (computed on read, N1/G2)", "doc" "kotoba Datom-log projection — first-class canonical state (ADR-2605312345)"} {"id" "cell:asobi.coverage", "method" "methods/coverage_report.py", "input" "data/seed-asobi-graph.kotoba.edn", "output" ["out/coverage-report.md"], "pipeline" "access + medium + domain + venue + enclosure → gap map", "doc" "honest coverage (all-culture ~0 by design); names thin/missing buckets per wave (G5)"}]
 :actor/gates [#:gate{:id "G1", :rule "PARTICIPATION / ACCESS map, NEVER an engagement / attention / popularity ranking — no retention metric, no recommend-for-time, no per-work popularity score; the 取-holder is the enclosure, the bearer is the play, the routing is opening"} #:gate{:id "G2", :rule "edge-primary (N1) — 取 lives only on :en/access-load; node openness is the integral computed on read; no :asobi/popularity-of-work"} #:gate{:id "G3", :rule "non-adjudicating (N3) — access categories are DISCLOSED facts, never asobi verdicts"} #:gate{:id "G4", :rule "public venue — open-source + on-chain + 1 SBT = 1 vote; never a private/covert registry"} #:gate{:id "G5", :rule "sourcing honesty — every record :authoritative | :representative; no fabricated coverage"} #:gate{:id "G6", :rule "Murakumo-only narration (ADR-2605215000)"} #:gate{:id "G7", :rule "outward-gated — live ingest (public event/license catalogs) requires Council + operator DID; R0 = analyzer + schema + seed only"} #:gate{:id "G8", :rule "no addictive design (Wellbecoming §1.13) — surfacing spectacle/retention design is unrepresentable"}]
 :actor/adr "2606073200"
 :actor/tier :tier-b
 :actor/integrates ["danjo" "inochi" "kanae" "kataribe" "kawaraban" "keizu" "manabi" "tsumugi"]
 :actor/purpose "Freed-time / play & cultural-expression (遊び) KG mirror — weaves cultural works / events / venues / practices + the enclosures that gate them into the kotoba Datom log; surfaces participation-openness vs enclosure routed to OPENING, never an engagement ranking."
 :actor/status :r0-design-only
 :actor/related ["2606073000" "2606011000" "2605081300" "2606061900" "2605301600" "2605312345" "2605215000" "2605241500"]
 :actor/id "asobi"}
